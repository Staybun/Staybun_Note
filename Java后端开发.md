# JavaSE(25/7/20-25/8/17)

## 一、前置知识

### 1. dos命令

-   windows的cmd命令行内操作是**dos命令**
-   Linux是在Terminal上的**bash/shell命令**

-   两者相似但不一样
-   dos使用GBK编码，即ANSI
-   常用dos命令

| 切换指定路径         | C:\Users\java\... |
| -------------------- | ----------------- |
| 查看当前路径下的内容 | dir               |
| 进入                 | cd                |
| 创建文件夹           | mkdir 文件夹名    |
| 上下键               | 复制上个命令      |
| 补全                 | Tab               |

### 2. Java环境

-   | 名称    | 全称                     | 是什么         | 作用               |
    | ------- | ------------------------ | -------------- | ------------------ |
    | **JVM** | Java Virtual Machine     | Java虚拟机     | 运行 `.class` 文件 |
    | **JRE** | Java Runtime Environment | Java运行环境   | JVM 和类库         |
    | **JDK** | Java Development Kit     | Java开发工具包 | JRE + 开发工具     |

-   JDK>JRE>JVM

-   ```
    a. 编写
    b. 编译：javac 文件名.java
            生成class文件
    c. 运行：java class文件名
    ```

### 3. IDEA-intelliJ

-   项目目录结构举例
    -   **project**-javase
    -   **module**-module3
        -   **src**-存放所有的 `.java` 文件
            -   **package**-推荐域名倒写:`www.zihao.com` -> `com.zihao.xxx`
                -   在package创建class

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20250722224236839.png" alt="image-20250722224236839" style="zoom: 67%;" />

-   Tab补全操作

    -   生成main方法:输入main
    -   输出print语句:sout

-   IDEA快捷键

    | 快捷键                   | 功能                                   |
    | ------------------------ | -------------------------------------- |
    | `Alt`+`Enter`            | ==导入包，自动修正代码==               |
    | `Ctrl`+`Y`               | ==删除光标所在行==                     |
    | `Ctrl`+`D`               | ==复制光标所在行的内容，插入光标下面== |
    | `Ctrl`+`Alt`+`L`         | 格式化代码                             |
    | `Ctrl`+`/`               | 单行注释                               |
    | `Ctrl`+`Shift`+`/`       | 选中代码注释，多行注释，再按取消注释   |
    | `Alt`+`Shift`+`上下箭头` | ==移动当前代码行==                     |
    | `Ctrl`+`r`               | ==替换==                               |

## 二、基础用法（25/7/20-25/7/27）

### 1. Scanner类

-   是什么? 类似python中的input自己输入

-   位于`java.util`包

    >   a.导入包
    >
    >   import java.util.Scanner;
    >
    >   b.创建对象
    >
    >   Scanner sc = new Scanner(System.in);
    >
    >   c.调用方法 

### 2. Random类

-   是什么? 用来生成随机数

-   位于`java.util`包

    >   a.导入包
    >
    >   import java.util.Random;
    >
    >   b.创建对象
    >
    >   Random rand = new Random();
    >
    >   c.调用方法 

| 方法           | 说明                                                         |
| -------------- | ------------------------------------------------------------ |
| `nextInt()`    | Scanner: 读取一个整数; Random: 随机生成一个int范围整数       |
| `nextDouble()` | Scanner: 读取一个小数; Random: 生成 `0.0` 到 `1.0` 之间的小数 |
| `nextLine()`   | Scanner: 读取字符串,遇到空格和回车结束                       |
| `next()`       | Scanner: 读取字符串,遇到回车结束                             |
| `nextInt(n)`   | 生成 `0` 到 `n-1` 之间的整数                                 |

### 3. 数组array

```java
int [] a = new int [10]; //数组动态
int [] b = {1,2,3,4,5};  //数组静态
int c = a.length;  //获取数组长度
```

### 4. 内存图

-   分为五块
    -   栈 (Stack) (★)
        -   主要运行方法
    -   堆 (Heap) (★)
        -   保存对象,数组开辟空间
        -   默认值如:0,0.0,'\u0000',false,null
        	-	成员变量有默认值，局部变量没有
    -   方法区 (Method Area) (★)
        -   主要保存class文件中的信息
        -   代码运行前,需要先进内存(方法区)
    -   本地方法栈
    -   寄存器

### 5. 方法

-   通用格式: 

```java
public static 返回值类型/void 方法名(参数){
    方法体
    return 返回值结果
}
```

-   方法的重载：调用方法名一样，形参顺序/数量/数据类型不一样

-   **可变参数**：数据类型...变量名

    -   本质是一个数组,和其他普通参数一起使用时,可变参数需要放到参数列表最后

    ```java
    public static void sum(int a,int...arr){
        方法体
        return 返回值结果
    }
    ```

-   快捷键：alt+enter

### 6. 面向对象

-   **类**
    -   **测试类**：带main方法，运行代码用
    -   **实体类**：自拟类
    -   组成
        -   **属性（成员变量）**：定义在**类中方法外**；作用于当前类
        -   **行为（成员方法）**：定义方法时候去掉`static`即可；带`static`表示调用不需要`new`

-   **对象**

    -   使用

        -   **导包**：`import 包名.类名`，同一包下的类，使用对方成员不需要导包

            >   特殊包：`java.lang` 比如 `String`不需要导包

        -   **创建对象**：`类名 对象名 = new 类名()`

            >   eg： Person zihao = new Person()

        -   **调用成员(成员变量，成员方法) **：用`.`调用即可

-   **匿名对象**

    >   new 对象().成员

### 7. 封装

-   **理解**：将细节隐藏，不让外界随便使用，对外提供接口

-   ==**关键字**==

    -   `private` 被其修饰后，只能在本类调用（属性私有）

        >   private int a;

    -   `this` 区分成员变量和局部变量，谁调用this，就代表哪个对象

        >   this.name = name;

    -   `static` 静态关键字，类名点直接调用，静态成员优先于非静态成员存在在内存中

        >   static int a;
        >
        >   public static void sum()

    -   不管在不在同一个类中,非静态成员都可以new对象调用

        不管在不在同一个类中,静态成员都可以类名调用

-   **get/set 方法**：对外提供公共接口

-   **构造**
    
    -   无参构造
    -   有参构造
    
-   **标准JavaBean**
    -   public具体的类
    -   属性私有
    -   有构造方法
    -   提供get/set 方法

-   **快捷键**：alt+insert

### 8. 递归

-   阶乘，斐波那契数列

### 9. 数组常见算法

-   数组翻转
-   冒泡排序
-   二分查找

### 10. 对象数组

```java
# 定义一个存储Person型的数组:Person[]
Person[] arr = new Person[3];
Person p = arr[i];
```

### 11. 方法参数

-   基本类型做方法参数传递,传递的是值,不是变量本身
    -   方法运行:压栈
        方法运行完毕:弹栈 -> 释放栈内存

-   引用数据类型做方法参数传递时,传递的是地址值

### 12. 命令行参数

-   ```java
    public static void main(String[] args)
    ```

-   <img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20250726220910721.png" alt="image-20250726220910721" style="zoom: 50%;" />

## 三、进阶用法(728-817)

### 1. 继承

#### a.关键字

```java
子类 extends 父类
public class Teacher extends Employee{
    
}
```

-   继承前提下

    -   **变量访问看左边（声明类型）**，方法访问看右边（对象类型）

    -   ```java
        class Parent {
            public int age = 50;
            public void sayHi() {
                System.out.println("父类 sayHi");
            }
        }
        
        class Child extends Parent {
            public int age = 20;
            @Override
            public void sayHi() {
                System.out.println("子类 sayHi");
            }
        }
        
        Parent p = new Child();			// 父类引用，子类对象
        System.out.println(p.age);     // 50 -> 变量看左边 Parent
        p.sayHi();                     // 子类 sayHi -> 方法看右边实际对象 Child
        
        ```

        

#### b.方法重写

-   继承前提下，子类中有一个和父类方法名以及参数列表相同的方法
-   检测是否为重写方法:在该方法上写`@Override  `

```java
public class Zi extends Fu{
    @Override
    public void method(){
        System.out.println("我是子类中的method方法");
    }

注意：私有方法不能被重写,构造方法不能被重写,静态方法不能被重写
```

#### c.super和this

-   new子类对象时,会先初始化父类(先走父类无参构造方法)
-    super()代表的是父类无参构造   （默认第一行，不写则jvm提供）

```java
super 使用:
  a.调用父类构造方法-> 在子类中的构造中写
    super() -> 调用父类无参构造
    super(实参)  -> 调用父类有参构造
      
  b.调用父类成员变量:
    super.成员变量名
        
  c.调用父类成员方法:
    super.成员方法名(实参)
        
this 使用:
  a.调用当前对象的构造:在构造中写
    this():调用当前对象的无参构造
    this(实参):调用当前对象的有参构造
  b.调用当前对象的成员变量:
    this.成员变量名
  c.调用当前对象的成员方法:
    this.成员方法名(实参)

注意:
  不管是super还是this,只要在构造中使用,都必须在第一行,所以二者不能同时手写出来
```

#### d.抽象

```java
关键字: abstract

抽象方法:
      修饰符 abstract 返回值类型 方法名(参数);

抽象类:
       public  abstract class 类名{}

注意:
       a.抽象方法所在的类一定是抽象类
       b.抽象类中不一定非得有抽象方法
       c.子类继承父类之后,需要重写父类中所有的抽象方法,不然编译报错
```

### 2.接口

#### a.关键字

-   是一个引用数据类型

```java
关键字:
   a.interface 接口
      public interface 接口名{}
   
  b.implements 实现
      实现类 implements 接口名{}
```

```java
1.定义接口:
  public interface 接口名{}
2.实现:
  public class 实现类类名 implements 接口名{}
3.使用:
  a.实现类实现接口
  b.重写接口中的抽象方法
  c.创建实现类对象(接口不能直接new对象)
  d.调用重写的方法   
```

#### b.接口中的成员

-   抽象方法

```java
1.定义格式:
  public abstract 返回值类型 方法名(形参);
2.注意:
  不写public abstract 默认也有
3.使用:
  a.定义实现类,实现接口
  b.重写抽象方法
  c.创建实现类对象,调用重写的方法
```

-   默认方法

```java
1.格式:
  public default 返回值类型 方法名(形参){
      方法体
      return 结果
  }
2.使用:
  a.定义实现类,实现接口
  b.默认方法可重写,可不重写
  c.创建实现类对象,调用默认方法   
```

-   静态方法

```java
1.定义格式:
  public static 返回值类型 方法名(形参){
      方法体
      return 结果
  } 
2.使用:
  接口名直接调用
```

-   **默认方法**和静态方法 -> 可以作为临时加的一个小功能来使用

#### c.成员变量

```java
1.格式:
  public static final 数据类型 变量名 = 值
2.相关知识点:final
  final代表最终的,被它修饰的变量,不能二次赋值,可以视为常量
3.特点:
  不写public static final 默认也有
4.使用:
  接口名直接调用
5.注意:
  a.被static final修饰的成员变量需要手动赋值
  b.习惯上我们会将static final修饰的成员变量名大写
```

```java
public interface Logger {
    void log(String message);            // 抽象方法

    default void logInfo(String msg) {   // 默认方法
        log("[INFO] " + msg);
    }

    static void logSystem(String msg) {  // 静态方法
        System.out.println("[SYSTEM] " + msg);
    }

    int DEFAULT_LEVEL = 1;               // 接口常量
}

public class FileLogger implements Logger {
    public void log(String message) {
        System.out.println("写入文件: " + message);
    }
}

public class Test {
    public static void main(String[] args) {
        FileLogger logger = new FileLogger();
        logger.logInfo("程序启动");
        Logger.logSystem("系统初始化完成");
        System.out.println(Logger.DEFAULT_LEVEL);
    }
}
```

#### d.接口特点

```java
1.接口可以多继承 -> 一个接口可以继承多个接口
  public interface InterfaceA extends InterfaceB,InterfaceC{}
2.接口可以多实现 -> 一个实现类可以实现一个或者多个接口
  public class InterfaceImpl implements InterfaceA,InterfaceB{}
3.一个子类可以继承一个父类的同时实现一个或者多个接口
  public class Zi extends Fu implements  InterfaceA,InterfaceB{}
4.注意:
  继承也好,实现接口也罢,只要是父类中或者接口的抽象方法,子类或者实现类都要重写
```

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20250729135945020.png" alt="image-20250729135945020" style="zoom: 67%;" />

### 3.多态

```java
1.前提:
  a.必须有子父类继承或者接口实现关系
  b.必须有方法的重写
  c.new对象:父类引用指向子类对象
    Fu fu = new Zi()
2.注意:
  多态模式下不能直接调用子类特有功能
```

-   多态的好处

```java
多态方式和原始方式new对象的优缺点:
  原始方式:
    a.优点:既能调用重写的,还能调用父类非私有的,还能调用自己特有的
    b.缺点:扩展性差       
  多态方式:
    a.优点:扩展性强
    b.缺点:不能直接调用子类特有功能
```

-   多态转型

    ```java
    1.向下转型:好比强转,将大类型强制转成小类型
    2.想要调用子类特有功能,需要向下转型  
    ```

    ```Java\
    1.如果等号左右两边类型不一致,会出现类型转换异常(ClassCastException)
    2.解决:
      在向下转型之前,先判断类型
    3.怎么判断类型: instanceof
      判断结果是boolean型
    4.使用:
      对象名 instanceof 类型 -> 判断的是关键字前面的对象是否符合关键字后面的类型
    ```

    ```java
    interface USB {
        void connect();
    }
    
    class Mouse implements USB {
        public void connect() {
            System.out.println("鼠标连接");
        }
    }
    
    class Keyboard implements USB {
        public void connect() {
            System.out.println("键盘连接");
        }
    }
    
    public class Main {
        public static void main(String[] args) {
            USB u1 = new Mouse();
            USB u2 = new Keyboard();
    
            u1.connect();  // 多态，执行 Mouse 的 connect()
            u2.connect();  // 多态，执行 Keyboard 的 connect()
        }
    }
    ```

### 4.权限修饰符

-   `public`：公共的,最高权限,被public修饰的成员,在哪里都能访问
-   protected：受保护的

-   default：:默认  不写权限修饰符就是默认权限,不能直接把default写出来

-   `private`：私有的,只能在自己的类中直接访问

### 5.final关键字

-   最终的

-   修饰类: 被final修饰的类不能被继承

-   修饰方法:被final修饰的方法,不能被重写**(比如不希望子类重写 `toString()` 之类的方法)**

-   修饰局部变量: 被final修饰的变量不能二次赋值**(常用于常量定义)**

### 6.代码块

-   构造代码块

```java
格式:
  {
      代码
  }
执行特点:优先于构造方法执行,每new一次,就会执行一次
```

-   **静态代码块**

```java
格式:
  static{
      代码
  }
执行特点: 静态代码块优先于构造代码块和构造方法执行的,而且只执行一次

如果想让一些数据最先初始化,而且只需要初始化一次,就可以将这些数据放到静态代码块中
```

```Java
public class App {
    static {
        System.out.println("应用启动前初始化一次");
    }

    public static void main(String[] args) {
        System.out.println("Main方法运行");
    }
}
```

### 7. 内部类

-   静态成员内部类

    ```java
    public class A{
          static class B{
              
          }
    }
    
    调用静态内部类成员:
      外部类.内部类 对象名 = new 外部类.内部类()
     Person.Heart heart = new Person.Heart();
    ```

-   非静态成员内部类

```java
  public class A{
      class B{
          
      }
  }

  外部类.内部类 对象名 = new 外部类().new 内部类()
 Person.Heart heart = new Person(). new Heart();
```

-   局部内部类

-   **匿名内部类**
    -   当简单调用一次接口中的方法,我们就可以使用匿名内部类

```java
格式:
  new 接口/抽象类(){
      重写方法
  }.重写的方法();

  =================================

  类名 对象名 = new 接口/抽象类(){
      重写方法
  }
  对象名.重写的方法();
```

```java
1.实现接口
Runnable r = new Runnable() {
    @Override
    public void run() {
        System.out.println("匿名内部类实现线程");
    }
};
new Thread(r).start();

2.继承类
abstract class Animal {
    abstract void eat();
}

Animal a = new Animal() {
    @Override
    void eat() {
        System.out.println("狗吃骨头");
    }
};
a.eat();
```

### 8.异常

#### a. API文档

-   Application Programming Interface,简称API,又称之为应用编程接口

-   方便我们去查询开发好的接口以及类,以及其中的方法 -> API文档

#### b. 异常

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20250801194606724.png" alt="image-20250801194606724" style="zoom: 67%;" />

-   **异常处理方式**
-   throws  处理“我不想处理”的异常

```java
1.格式:在方法参数和方法体之间位置上写
  throws 异常
2.意义:处理异常
  将异常往上抛
3.创建异常 
  throw new 异常
多个异常
1.格式:throws 异常1,异常2
    
2.注意:
  如果throws的多个异常之间有子父类继承关系,我们可以直接throws父类异常
  如果不知道多个异常之间是否有子父类继承关系,我们可以直接throws Exception    
```

-   try...catch 常用

```java
1.格式:
  try{
      可能出现异常的代码
  }catch(异常 对象名){
      处理异常的代码-> 将来开发会将异常信息保存到日志文件中
  }

2.多个异常
  try{
      可能出现异常的代码
  }catch(异常 对象名){
      处理异常的代码-> 将来开发会将异常信息保存到日志文件中
  }catch(异常 对象名){
      处理异常的代码-> 将来开发会将异常信息保存到日志文件中
  }catch(异常 对象名){
      处理异常的代码-> 将来开发会将异常信息保存到日志文件中
  }catch(异常 对象名){
      处理异常的代码-> 将来开发会将异常信息保存到日志文件中
  }...
```

-   finally关键字

    ```java
    1.概述:代表的是不管是否触发了异常,都会执行的代码块
    2.使用:配合try...catch使用
      try{
          可能出现异常的代码
      }catch(异常 对象名){
          处理异常的代码-> 将来开发会将异常信息保存到日志文件中
      }finally{
          不管是否有异常,都会执行的代码
      }  
    
    3.应用场景:如果打开了文件、连接了数据库等资源，即使发生异常，也一定要关闭资源，否则会引起系统资源泄露。
    ```

-   自定义异常:

    -   开发中习惯用 `e` 定义异常名字

-   举例

    ```java
    public class Demo1 {
        public static void main(String[] args) {
            try {
                int num = 10 / 0; // 会发生 ArithmeticException
                System.out.println("正常执行的代码：" + num);
            } catch (ArithmeticException e) {
                System.out.println("捕获到异常：除以0了！");
            }
            System.out.println("程序继续执行...");
        }
    }
    
    
    public class Demo2 {
        public static void main(String[] args) {
            try {
                readFile();  // 调用者必须处理异常
            } catch (IOException e) {
                System.out.println("main里处理了异常：" + e.getMessage());
            }
        }
    
        public static void readFile() throws IOException {
            throw new IOException("文件读取失败");
        }
    }
    
    ```

-   打印异常

```java
Throwable类中的方法:
  String toString()  :输出异常类型和设置的异常信息
  String getMessage(): 输出设置的异常信息  
  void printStackTrace():打印异常信息是最全的:包括异常类型,信息,以及出现的行数等    
      
 if (name.equals(username)) {
            System.out.println("登录成功了");
        } else {
            try {
                throw new LoginUserException("登录失败了,用户名或者密码有问题");
            }catch (Exception e){
                //System.out.println(e.toString());
                //System.out.println(e.getMessage());
                e.printStackTrace();
            }
```

### 9. Object类

#### a. toString方法

-   用于返回对象的字符串表示形式，默认返回的是类名 + 哈希地址（地址值）
-   用于打印对象内容

```java
// 原始方法------输出地址值
public String toString() {
    return getClass().getName() + "@" + Integer.toHexString(hashCode());
}

// 重写方法(alt+insert)------输出对象内容
    public String toString() {
        return "Animal{" +
                "name='" + name + '\'' +
                ", age=" + age +
                '}';
    }
```

#### b. equals方法

-   默认是比较对象地址是否一样=；重写之后可以用来比较对象内容。

-   判断两个对象是否相等

```java
== 针对于基本数据类型来说,比较的是值 
== 针对于引用数据类型来说,比较的是地址值

//原始方法------比较地址值
public boolean equals(Object obj) {
    return (this == obj);
}

//重写方法-----比较对象内容
    public boolean equals(Object o) {
        if (o == null || getClass() != o.getClass()) return false;
        Animal animal = (Animal) o;
        return age == animal.age && Objects.equals(name, animal.name);
    }
```

-   alt+insert -> 选 `toString` 或者`equals and hashcode	`

#### c. 比较方法

| 比较项       | Comparable                 | Comparator                         |
| ------------ | -------------------------- | ---------------------------------- |
| 定义位置     | 类内部实现 `compareTo()`   | 类外实现 `compare(obj1, obj2)`     |
| 是否修改类   | ✅ 需要修改类               | ❌ 不修改类                         |
| 排序规则数量 | ❌ 只能有一个               | ✅ 可定义多个                       |
| 适用场景     | 排序逻辑固定，如：按ID排序 | 排序逻辑灵活，如：按年龄、按名字等 |
| 常用写法     | `implements Comparable<T>` | `Comparator<T>` / Lambda表达式     |

### 10. 基础API

#### a. String

| 方法                                    | 类型   | 用途           | 实用等级   | 示例                      |
| --------------------------------------- | ------ | -------------- | ---------- | ------------------------- |
| `equals()`                              | String | 内容比较       | ⭐⭐⭐⭐⭐      | `s1.equals(s2)`           |
| `equalsIgnoreCase()`                    | String | 忽略大小写比较 | ⭐⭐⭐⭐ 常用  | `s1.equalsIgnoreCase(s2)` |
| `length()`                              | String | 获取字符串长度 | ⭐⭐⭐⭐⭐ 必学 | `s.length()`              |
| `charAt(int)`                           | String | 获取指定字符   | ⭐⭐⭐⭐ 常用  | `s.charAt(0)`             |
| `indexOf(String)`                       | String | 查找子串下标   | ⭐⭐⭐⭐ 常用  | `s.indexOf("abc")`        |
| `substring(int)` / `substring(int,int)` | String | 截取子串       | ⭐⭐⭐⭐⭐ 必学 | `s.substring(1,4)`        |
| `toLowerCase()` / `toUpperCase()`       | String | 大小写转换     | ⭐⭐⭐ 常用   | `s.toLowerCase()`         |
| `trim()`                                | String | 去掉首尾空格   | ⭐⭐⭐⭐⭐ 必学 | `s.trim()`                |
| `split(String)`                         | String | 字符串分割     | ⭐⭐⭐⭐⭐ 必学 | `s.split(",")`            |
| `contains(String)`                      | String | 是否包含子串   | ⭐⭐⭐⭐ 常用  | `s.contains("abc")`       |
| `startsWith()` / `endsWith()`           | String | 判断前缀/后缀  | ⭐⭐⭐ 常用   | `s.startsWith("http")`    |
| `replace(char, char)`                   | String | 替换字符       | ⭐⭐⭐⭐ 常用  | `s.replace('a','b')`      |

| 构造方式           | 是否常用  | 示例               |
| ------------------ | --------- | ------------------ |
| `String(char[])`   | ⭐⭐⭐ 常用  | 字符数组变字符串   |
| `String(byte[])`   | ⭐⭐ 偶尔用 | 比如网络或编码处理 |

```java
判等用 equals，
查找靠 indexOf；
截取用 substring，
长度 length 不能忘；
拼接就用 append，
转成字符串 toString；
大小写记得变，
空格 trim 不留边。
```

#### b. StringBuilder

```java
1.概述:一个可变的字符序列,此类提供了一个与StringBuffer兼容的一套API,但是不保证同步(线程不安全,效率高)
2.作用:主要是字符串拼接
3.问题:
  a.刚讲完String,String也能做字符串拼接,直接用+即可,但是为啥还要用StringBuilder去拼接呢?
  b.原因:
    String每拼接一次,就会产生新的字符串对象,就会在堆内存中开辟新的空间,如果拼接次数多了,会占用内存,效率比较底   
    StringBuilder,底层自带一个缓冲区(没有被final修饰的byte数组)拼接字符串之后都会在此缓冲区中保存,在拼接的过程中,不会随意产生新对象,节省内存
```

| 方法         | 用途       | 示例               |
| ------------ | ---------- | ------------------ |
| `append()`   | 添加内容   | `sb.append("abc")` |
| `toString()` | 转回字符串 | `sb.toString()`    |
| `reverse()`  | 反转字符串 | `sb.reverse()`     |

```java
StringBuilder sb = new StringBuilder();
for (int i = 0; i < 5; i++) {
    sb.append(i);
}
System.out.println(sb.toString()); // "01234"
```

#### c. Date类

| 类名                | 实用性 | 推荐等级 | 说明                                       |
| ------------------- | ------ | -------- | ------------------------------------------ |
| `SimpleDateFormat`  | ✅常用  | ⭐⭐⭐⭐     | **老项目中非常常见**，日期格式化/解析利器  |
| `LocalDate`         | ✅常用  | ⭐⭐⭐⭐⭐    | JDK8新特性，专门操作年月日，推荐           |
| `LocalDateTime`     | ✅常用  | ⭐⭐⭐⭐⭐    | 操作“年月日时分秒”，是未来主流写法         |
| `Period/Duration`   | 🔍了解  | ⭐⭐⭐      | 用于**两个时间的差值计算**，常用于业务逻辑 |
| `DateTimeFormatter` | ✅常用  | ⭐⭐⭐⭐     | 新API对应的日期格式化工具                  |
```java
1. LocalDate/LocalDateTime（JDK 8+新API）
// 获取当前时间
LocalDateTime now = LocalDateTime.now();
// 创建指定时间
LocalDate birthday = LocalDate.of(1999, 8, 6);
// 向后偏移10天
LocalDate after10Days = birthday.plusDays(10);
// 获取字段
int year = now.getYear();

2.DateTimeFormatter（JDK 8新API中格式化日期）
DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss");
String timeStr = formatter.format(LocalDateTime.now()); // 转成字符串

3.SimpleDateFormat（老API中格式化字符串）
SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
String timeStr = sdf.format(new Date()); // Date -> String
Date date = sdf.parse("2025-08-06 14:30:00"); // String -> Date

4.Period 和 Duration（计算时间差）
LocalDate today = LocalDate.now();
LocalDate deadline = LocalDate.of(2025, 12, 1);
// 计算日期之间的天数差
Period period = Period.between(today, deadline);
System.out.println(period.getMonths()); // 剩余几个月
```

#### d. Math类

| 方法                    | 功能                    | 举例                     |
| ----------------------- | ----------------------- | ------------------------ |
| `abs(x)`                | 绝对值                  | `Math.abs(-5) -> 5`      |
| `ceil(x)`               | 向上取整（返回 double） | `Math.ceil(4.1) -> 5.0`  |
| `floor(x)`              | 向下取整（返回 double） | `Math.floor(4.9) -> 4.0` |
| `round(x)`              | 四舍五入（返回 long）   | `Math.round(4.5) -> 5`   |
| `max(a,b)` / `min(a,b)` | 最大最小值              | `Math.max(10, 20) -> 20` |

-   **BigInteger 类**: 用来处理**超过 long 范围的大整数**

-   **BigDecimal类**: 处理**小数精度问题**

```java
import java.math.BigDecimal;

public class Demo {
    public static void main(String[] args) {
        BigDecimal a = new BigDecimal("0.1");
        BigDecimal b = new BigDecimal("0.2");

        BigDecimal sum = a.add(b);
        System.out.println("相加结果: " + sum); // 0.3

        // 除法：保留2位小数，四舍五入
        BigDecimal divide = a.divide(new BigDecimal("0.03"), 2, BigDecimal.ROUND_HALF_UP);
        System.out.println("相除结果: " + divide);
    }
}
```

#### e. 工具类

| 方法                  | 实用性 | 常用程度 | 用途说明                                                |
| --------------------- | ------ | -------- | ------------------------------------------------------- |
| `currentTimeMillis()` | ⭐⭐⭐⭐   | **常用** | 用于**计算代码执行耗时**、记录时间戳                    |
| `exit(int status)`    | ⭐⭐     | **少用** | 程序提前退出。`exit(0)` 表示正常退出                    |
| `arraycopy(...)`      | ⭐⭐⭐    | **中等** | 用于数组复制，比 for 循环效率高，但初学可以不用优先掌握 |

```java
long start = System.currentTimeMillis();
for (int i = 0; i < 1000000; i++) {
    // 模拟一些逻辑
}
long end = System.currentTimeMillis();
System.out.println("耗时：" + (end - start) + "ms");

int[] src = {1, 2, 3, 4, 5};
int[] dest = new int[5];
System.arraycopy(src, 0, dest, 0, src.length);
```

| 方法                                    | 实用性 | 常用程度     | 说明                               |
| --------------------------------------- | ------ | ------------ | ---------------------------------- |
| `toString(int[] a)`                     | ⭐⭐⭐⭐⭐  | **非常常用** | 把数组内容变成字符串，方便打印输出 |
| `sort(int[] a)`                         | ⭐⭐⭐⭐⭐  | **非常常用** | 升序排序                           |
| `binarySearch(int[] a, int key)`        | ⭐⭐     | **中等偏低** | 二分查找，数组必须是**已排序的**   |
| `copyOf(int[] original, int newLength)` | ⭐⭐⭐    | **中等常用** | 数组扩容（比如动态添加元素时使用） |

```java
int[] nums = {4, 2, 9, 1};
Arrays.sort(nums);
System.out.println(Arrays.toString(nums)); // [1, 2, 4, 9]

int[] nums = {1, 3, 5, 7, 9};
int index = Arrays.binarySearch(nums, 5);
System.out.println("5的位置：" + index); // 输出2

int[] oldArr = {1, 2, 3};
int[] newArr = Arrays.copyOf(oldArr, 5);  // newArr = [1, 2, 3, 0, 0]
```

#### f. 包装类

```java
int -> Integer
double -> Double
boolean -> Boolean
...
```

-   **包装类的作用：让基本类型也能像对象一样使用方法**

-   **实际用途：用于集合框架、工具类操作等**

```java
ArrayList<int> list = new ArrayList<>(); // ❌报错，不能用基本类型
ArrayList<Integer> list = new ArrayList<>(); // ✅用包装类
```

```java
Integer.valueOf(int i)         // 装箱
int i = integer.intValue();    // 拆箱
//实际开发中，Java 会自动装箱拆箱
Integer a = 10;   // 自动装箱，相当于 Integer.valueOf(10)
int b = a;        // 自动拆箱，相当于 a.intValue()
```

-   基本类型和String的转换: `parsexxx`

    ```java
    // 基本类型 -> String
    String s1 = 123 + "";
    String s2 = String.valueOf(123);
    // String -> 基本类型
    int i = Integer.parseInt("123");
    double d = Double.parseDouble("3.14");
    
    //例子：用户输入转为数值
    Scanner scanner = new Scanner(System.in);
    System.out.print("请输入年龄: ");
    String ageStr = scanner.nextLine();
    int age = Integer.parseInt(ageStr); // 将字符串转为整数
    ```

-   `JavaBean` 中属性使用包装类

```java
public class User {
    private Integer age;  // 不用int
    private Boolean active;
}
```

### 11. 多线程

#### a. 基本概念

-   进程:在内存中执行的应用程序
-   线程:是进程中最小的执行单元
-   并发:同一个时刻,有多个执行在多个CPU上(同时)执行
-   并行:在同一个时刻,有多个指令在单个CPU上(交替)执行

#### b. 创建线程方法

-   方法1: 继承`Thread`

```java
1.定义一个类,继承Thread
2.重写run方法,在run方法中设置线程任务
3.创建自定义线程类的对象 
4.调用Thread中的start方法,开启线程,jvm自动调用run方法
//两个线程交替执行任务
public class MyThread extends Thread {
    public void run() {
        for (int i = 0; i < 3; i++) {
            System.out.println(getName() + " 正在执行任务：" + i);
        }
    }

public static void main(String[] args) {
        MyThread t1 = new MyThread(); // 线程1
        MyThread t2 = new MyThread(); // 线程2
        t1.setName("线程A");
        t2.setName("线程B");
        t1.start(); // 启动线程
        t2.start();
    }
}
```

| 功能                              | 是否常用    | 用途说明                               |
| --------------------------------- | ----------- | -------------------------------------- |
| `start()`                         | ✅必须掌握   | 启动线程                               |
| `run()`                           | ✅必须掌握   | 设置任务代码（重写方法）               |
| `sleep()`                         | ✅常用       | 模拟网络延迟、限速                     |
| `currentThread()`                 | ✅常用       | 获取当前线程对象，调试时很有用         |
| `getName()` / `setName()`         | ✅常用       | 给线程取名，便于日志排查问题           |
| `join()`                          | ✅常用       | 等一个线程执行完再执行后续操作（插队） |
| `setDaemon()`                     | ⚠️有用但少用 | 守护线程，如：JVM垃圾回收线程          |
| `yield()`                         | ❓了解即可   | 礼让CPU                                |
| `setPriority()` / `getPriority()` | ❓了解即可   | 线程优先级:1,5,10                      |

-   **方法2: 实现接口Runnable**
    -   实现接口的方式更灵活，可以同时继承父类、实现多个接口

```java
public class MyTask implements Runnable {
    public void run() {
        System.out.println("执行线程任务：" + Thread.currentThread().getName());
    }
}

public class Test {
    public static void main(String[] args) {
        MyTask task = new MyTask();
        Thread thread = new Thread(task);
        thread.start();
    }
}
```

-   方法3 : 匿名创建

```java
public class Test {
    public static void main(String[] args) {
        Thread thread = new Thread(new Runnable() {
            public void run() {
                System.out.println("匿名线程：" + Thread.currentThread().getName());
            }
        });
        thread.start();
    }
}
```

-   **方法4: 实现Callable接口**

    ```java
    Callable<V>接口
    V call()  -> 设置线程任务的,类似于run方法
    获取call方法返回值: FutureTask<V>
      a. FutureTask<V> 实现了一个接口: Future <V>
      b. FutureTask<V>中有一个方法:
         V get() -> 获取call方法的返回值
             
    好处:有返回值,可以抛异常
    ```

-   **方法5:线程池**

    `Executors.newFixedThreadPool()` 创建固定大小线程池

    `submit(Runnable)` / `submit(Callable)` 提交任务

    `Future.get()` 获取结果

    `shutdown()` 关闭线程池

 ####  c. 线程安全

-   同步: 就是让多线程**有秩序地访问共享资源**，避免出现数据不一致或逻辑混乱的情况。**"锁"**的概念,同步的本质是**加锁**

```java
//同步代码块
public void increment() {
    synchronized(this) { // 锁对象一般选共享数据的持有者
        count++;
    }
}

//同步方法
public synchronized void increment() { // 等价于同步代码块锁this
    count++;
}

//静态同步方法
synchronized(类名.class)
```

#### d. 六种状态

| 状态                          | 通俗解释                                          | 例子                                                         |
| ----------------------------- | ------------------------------------------------- | ------------------------------------------------------------ |
| **NEW（新建）**               | 线程对象造好了，但发动机还没打火                  | `Thread t = new Thread(...);` 还没 `start()`                 |
| **Runnable（可运行）**        | 已经报了名，随时能上场，但要等裁判（CPU调度）喊你 | 操作系统的“待执行队列”里排队                                 |
| **Blocked（锁阻塞）**         | 想进屋干活，但门（锁）被别人反锁了                | 两个线程抢同一个 `synchronized` 锁，一个先拿到，另一个只能等 |
| **Waiting（无限等待）**       | 自己主动坐下等别人来叫，没闹钟                    | 调了 `wait()`，必须有人 `notify()`/`notifyAll()` 才能醒      |
| **Timed Waiting（计时等待）** | 也是等，但设了闹钟，到点自己醒                    | `sleep(2000)`、`wait(5000)`、`join(1000)` 这些带超时的       |
| **Terminated（终止）**        | 线程干完活或被意外砍掉，彻底结束                  | `run()` 方法结束了，或者异常没处理直接挂了                   |

#### e. 多线程唤醒

| 方法             | 说明                                                         |
| ---------------- | ------------------------------------------------------------ |
| void wait()      | 线程等待,等待的过程中线程会释放锁,需要被其他线程调用notify方法将其唤醒,重新抢锁执行 |
| void notify()    | 线程唤醒,一次唤醒一个等待线程;如果有多条线程等待,则随机唤醒一条等待线程 |
| void notifyAll() | 唤醒所有等待线程                                             |

>   wait和notify方法需要锁对象调用,必须是同一个锁对象,不能新new对象,可使用构造方法

#### f. Lock锁

```java
1.概述:Lock是一个接口
2.实现类:ReentrantLock 
3.方法:
  lock() 获取锁
  unlock() 释放锁
      
synchronized：像一个自动门，人（线程）来了，锁自己上；人走了，锁自己关。
Lock：像一把手动钥匙，你想什么时候开门就什么时候开，想什么时候关就什么时候关，而且你还可以检查门的状态。
```

#### j. 定时器

```java
1.构造:
  Timer()
2.方法:
  void schedule(TimerTask task, Date firstTime, long period)  
                task:抽象类,是Runnable的实现类
                firstTime:从什么时间开始执行
                period: 每隔多长时间执行一次,设置的是毫秒值    
```

### 12. 集合

#### a. Collection 接口

```java
1.Collection<E> 对象名 = new 实现类对象<E>()
2.<E>:泛型,决定了集合中能存储什么类型的数据,可以统一元素类型,只能写引用数据类型
```

-   **常用API**：`add`, `addAll`, `remove`, `clear`, `size`, `isEmpty`, `contains`, `toArray`

#### b. 迭代器

```java
1.概述:Iterator接口
2.主要作用:遍历集合
3.获取:
  Iterator<E> it = coll.iterator();
4.方法:
  boolean hasNext()  -> 判断集合中有没有下一个元素
  E next()  ->获取下一个元素    
```

#### c. List接口

```java
1.概述:是Collection接口的子接口
2.常见的实现类:
  ArrayList LinkedList Vector
```

##### ArrayList集合

```java
1.概述:ArrayList是List接口的实现类,底层结构是动态数组
2.特点:
  a.元素有序
  b.元素可重复
  c.有索引-> 可以利用索引去操作元素
  d.线程不安全
      
3.数据结构:数组       
4.常用方法:
  boolean add(E e)  -> 将元素添加到集合中->尾部(add方法一定能添加成功的,所以我们不用boolean接收返回值)
  void add(int index, E element) ->在指定索引位置上添加元素
  boolean remove(Object o) ->删除指定的元素,删除成功为true,失败为false
  E remove(int index) -> 删除指定索引位置上的元素,返回的是被删除的那个元素
  E set(int index, E element) -> 将指定索引位置上的元素,修改成后面的element元素
  E get(int index) -> 根据索引获取元素
  int size()  -> 获取集合元素个数
```

##### LinkedList集合

```java
1.概述:LinkedList是List接口的实现类,底层结构是双向链表
2.特点:
  a.元素有序
  b.元素可重复
  c.有索引 -> 这里说的有索引仅仅指的是有操作索引的方法,不代表本质上具有索引
  d.线程不安全
      
3.数据结构:双向链表      
    
4.方法:有大量直接操作首尾元素的方法
  - public void addFirst(E e):将指定元素插入此列表的开头。
  - public void addLast(E e):将指定元素添加到此列表的结尾。
  - public E getFirst():返回此列表的第一个元素。
  - public E getLast():返回此列表的最后一个元素。
  - public E removeFirst():移除并返回此列表的第一个元素。
  - public E removeLast():移除并返回此列表的最后一个元素。
  - public E pop():从此列表所表示的堆栈处弹出一个元素。
  - public void push(E e):将元素推入此列表所表示的堆栈。
  - public boolean isEmpty()：如果列表没有元素，则返回true。
```

#### d. 增强for

-   用来遍历集合或者数组

```java
public class Demo01ForEach {
    public static void main(String[] args) {
        ArrayList<String> list = new ArrayList<>();
        list.add("张三");
        list.add("李四");
        list.add("王五");
        list.add("赵六");
        for (String s : list) {
            System.out.println(s);
        }

        int[] arr = {1,2,3,4,5};
        for (int i : arr) {
            System.out.println(i);
        }
    }
}

1.增强for遍历集合时,底层实现原理为迭代器
2.增强for遍历数组时,底层实现原理为普通for
```

#### e. Collections工具类

```java
1.概述:集合工具类
2.特点:
  a.构造私有
  b.方法都是静态的
      
3.使用:类名直接调用
    
4.方法:
  static <T> boolean addAll(Collection<? super T> c, T... elements)->批量添加元素 
  static void shuffle(List<?> list) ->将集合中的元素顺序打乱
  static <T> void sort(List<T> list) ->将集合中的元素按照默认规则排序 
  static <T> void sort(List<T> list, Comparator<? super T> c)->将集合中的元素按照指定规则排序 (用到Comparator比较器)
```

#### f. 泛型

```java
1.泛型:<>
2.作用:
  统一数据类型,防止将来的数据转换异常
3.注意:
  a.泛型中的类型必须是引用类型
  b.如果泛型不写,默认类型为Object    
```

#### g. Set接口

##### HashSet

```java
1.概述:HashSet是Set接口的实现类
2.特点:
  a.元素唯一
  b.元素无序
  c.无索引
  d.线程不安全
3.数据结构:哈希表
  a.jdk8之前:哈希表 = 数组+链表
  b.jdk8之后:哈希表 = 数组+链表+红黑树
            加入红黑树目的:查询快
```

##### LinkedHashSet

```java
1.概述:LinkedHashSet extends HashSet
2.特点:
  a.元素唯一
  b.元素有序
  c.无索引
  d.线程不安全
3.数据结构:
  哈希表+双向链表  
```

##### TreeSet

```java
1.有序集合（元素会自动排序）
2.不能重复    
3.常见场景:排序去重;做一些需要“自动排序”的数据结构，比如排行榜
4.排序方式
    默认：自然顺序
    自定义：传入 Comparator 来指定排序规则
5.去重规则:比较结果相等就认为是同一个元素（不是依靠 equals，而是依靠比较器）
```

#### h. 哈希值

```java
1.概述:是由计算机算出来的一个十进制数,可以看做是对象的地址值
  a.哈希值不一样,内容肯定不一样
  b.哈希值一样,内容也有可能不一样    
```

-   存储去重复

```java
重写 hashCode + equals
快速定位元素位置 + 配合 equals 判断是否相同,去重
```

#### I. Map-双列集合

-   概述:是双列集合的顶级接口
-   元素特点: 元素都是由key(键),value(值)组成 -> 键值对

##### HashMap

```java
1.概述:HashMap是Map的实现类
2.特点:
  a.key唯一,value可重复 -> 如果key重复了,会发生value覆盖
  b.无序
  c.无索引
  d.线程不安全
  e.可以存null键null值
3.数据结构:
  哈希表
4.方法:
  V put(K key, V value)  -> 添加元素,返回的是
  V remove(Object key)  ->根据key删除键值对,返回的是被删除的value
  V get(Object key) -> 根据key获取value
  boolean containsKey(Object key)  -> 判断集合中是否包含指定的key
  Collection<V> values() -> 获取集合中所有的value,转存到Collection集合中
      
  Set<K> keySet()->将Map中的key获取出来,转存到Set集合中  
  Set<Map.Entry<K,V>> entrySet()->获取Map集合中的键值对,转存到Set集合中
```

##### LinkedHashMap

```java
1.概述:LinkedHashMap extends HashMap
2.特点:
  a.key唯一,value可重复 -> 如果key重复了,会发生value覆盖
  b.有序
  c.无索引
  d.线程不安全
  e.可以存null键null值
3.数据结构:
  哈希表+双向链表
4.使用:和HashMap一样      
```

##### 哈希表结构存储过程

```java
1.HashMap 底层核心是哈希表（数组 + 链表 / 红黑树）
2.put 存数据时的三个比较结果
哈希值不同 → 直接存
哈希值相同、内容不同 → 哈希冲突，放到链表/红黑树里
哈希值相同、内容相同 → 认为是同一个 key，覆盖旧的 value
3. hashCode() 和 equals() 的作用
要实现 key 的去重和比较，就得同时重写这两个方法（比如存自定义对象当 key）
4.扩容机制大概概念
默认数组长度：16
默认加载因子：0.75（装到 75% 触发扩容）
扩容倍数：×2
5.红黑树出现的条件
同一个索引上的链表长度 ≥ 8 且数组长度 ≥ 64，就转红黑树（提高查询速度）
```

##### TreeMap

```java
1.键值对（Key-Value），键有序
2.Key 不能重复，且根据 Key 排序
3.适合做区间查找,范围查询:
	headMap(toKey)：获取比某个 Key 小的所有键值对
    tailMap(fromKey)：获取比某个 Key 大的所有键值对
    subMap(fromKey, toKey)：范围区间查询
```

#### j. Properties集合

```java
1.概述:Properties 继承自 Hashtable
2.数据结构:哈希表
3.特有方法:
  Object setProperty(String key, String value)  -> 存键值对
  String getProperty(String key)  ->根据key获取value的
  Set<String> stringPropertyNames()  -> 获取所有的key,保存到set集合中,相当于keySet方法
  void load(InputStream inStream) -> 将流中的数据加载到Properties集合中(IO部分讲)   
4.常用于配置文件读取和写入
```

#### k. 集合嵌套

-   一个集合里再放集合

-   嵌套+遍历

    ```java
    ArrayList<ArrayList<String>>
    HashMap<String, ArrayList<String>>
    
    Map<String, List<String>> country = new HashMap<>();
    country.put("中国", Arrays.asList("北京", "上海", "广州"));
    country.put("日本", Arrays.asList("东京", "大阪"));
    
    for (String c : country.keySet()) {
        System.out.println(c + " -> " + country.get(c));
    }
    ```


#### m. 集合框架

-   **常用集合**

    -   **ArrayList** → 最常用的动态数组，查找快，增删慢（适合多查少改）。

    -   **LinkedList** → 链表结构，增删快，查找慢（适合多改少查）。

    -   **HashSet** → 无序、去重。

    -   **LinkedHashSet** → 有序、去重。

    -   **TreeSet** → 自动排序、去重。

    -   **HashMap** → 最常用的键值对集合，快速查找。

    -   **LinkedHashMap** → 有序的键值对。

    -   **TreeMap** → 按键排序的键值对。

    -   **Properties** → 读写 `.properties` 配置文件。

-   **集合的选用**
    -   想要 **有顺序且允许重复** → `List`
    -   想要 **去重** → `Set`
    -   想要 **键值对存储** → `Map`
    -   想要 **自动排序** → `TreeSet` / `TreeMap`
    -   想要 **配置文件** → `Properties`

### 13. IO流

#### a. File类

-   构造方法

```java
new File(String path)
```

```java
常用方法
String getAbsolutePath() -> 获取File的绝对路径->带盘符的路径
String getPath() ->获取的是封装路径->new File对象的时候写的啥路径,获取的就是啥路径
String getName()  -> 获取的是文件或者文件夹名称
long length() -> 获取的是文件的长度 -> 文件的字节数   
boolean createNewFile()  -> 创建文件
boolean mkdirs() -> 创建文件夹(目录)既可以创建多级文件夹,还可以创建单级文件夹
boolean delete()->删除文件或者文件夹
boolean isDirectory() -> 判断是否为文件夹 
boolean isFile()  -> 判断是否为文件
boolean exists()  -> 判断文件或者文件夹是否存在   
String[] list() -> 遍历指定的文件夹,返回的是String数组 
File[] listFiles()-> 遍历指定的文件夹,返回的是File数组 ->这个推荐使用
```

#### b. 字节流与字符流

-   **IO四大基类**（`InputStream`、`OutputStream`、`Reader`、`Writer`）

| 维度     | 字节流（`InputStream` / `OutputStream`）          | 字符流（`Reader` / `Writer`）                      |
| -------- | ------------------------------------------------- | -------------------------------------------------- |
| 处理单位 | **1 字节（8 位）**                                | **2 字节（16 位，UTF-16）**                        |
| 适用场景 | 二进制数据（图片、视频）,网络传输（Socket、HTTP） | 处理文本数据（txt、java、xml、json）,配置文件,日志 |
| 性能     | 原始字节操作，效率高                              | 先把字节解码成字符，效率略低                       |
| 区别     | 最小的存储&传输单元                               | 字符只表示**文字**（包括字母、汉字、符号等）       |
|                                      |                                                   |                                                    |
| **方法**                             | **适用类**                                        | **作用**                                           |
| **`read()`**                         | `InputStream` / `Reader`               | 读一个字节 / 字符（返回 int，-1 表示结束） |
| **`read(byte[]/char[])`**            | 同上                                   | 一次读多个字节 / 字符，效率比单个高        |
| **`write(int)`**                     | `OutputStream` / `Writer`              | 写一个字节 / 字符                          |
| **`write(byte[]/char[])`**           | 同上                                   | 一次写多个字节 / 字符                      |
| **`write(byte[]/char[], off, len)`** | 同上                                   | 写数组的一部分                             |
| **`flush()`**                        | 主要在 `Writer` / `OutputStreamWriter` | 刷新缓冲区，确保数据写入目标               |
| **`close()`**                        | 所有流                                 | 释放资源（自动 flush）                     |

-   代码模板

    ```java
    ① 读取文件（字节流 / 二进制）
    try (FileInputStream fis = new FileInputStream("a.jpg")) {
        byte[] buf = new byte[1024];
        int len;
        while ((len = fis.read(buf)) != -1) {
            // 处理数据
        }
    } catch (IOException e) {
        e.printStackTrace();
    }
    ② 写文件（字节流）
    try (FileOutputStream fos = new FileOutputStream("b.jpg", true)) { // true = 追加
        byte[] data = "Hello".getBytes();
        fos.write(data);
    } catch (IOException e) {
        e.printStackTrace();
    }
    ③ 读取文本（字符流）
    try (FileReader fr = new FileReader("a.txt")) {
        char[] buf = new char[1024];
        int len;
        while ((len = fr.read(buf)) != -1) {
            System.out.print(new String(buf, 0, len));
        }
    }
    ```


#### c. 缓冲流

-   **字节缓冲流**：`BufferedInputStream` / `BufferedOutputStream`;  复制图片、视频、压缩包等二进制文件

-   **字符缓冲流**：`BufferedReader` / `BufferedWriter`;  读写文本

```java
try (
    BufferedInputStream bis = new BufferedInputStream(new FileInputStream("input.mp4"));
    BufferedOutputStream bos = new BufferedOutputStream(new FileOutputStream("output.mp4"))
) {
    byte[] buffer = new byte[8192]; // 默认缓冲区大小
    int len;
    while ((len = bis.read(buffer)) != -1) {
        bos.write(buffer, 0, len);
    }
} // 自动 close


try (BufferedReader br = new BufferedReader(new FileReader("input.txt"));
     BufferedWriter bw = new BufferedWriter(new FileWriter("output.txt", true))) {

    String line;
    while ((line = br.readLine()) != null) {
        bw.write(line);
        bw.newLine(); // 换行
    }
}
```

#### d. 转换流

-   把**字节流**和**字符流**之间互相转换，可以**指定字符编码**（解决乱码问题）
-   `InputStreamReader/OutputStreamWriter`

```java
try (
    InputStreamReader isr = new InputStreamReader(new FileInputStream("gbk.txt"), "GBK");
    BufferedReader br = new BufferedReader(isr)
) {
    String line;
    while ((line = br.readLine()) != null) {
        System.out.println(line);
    }
}
```

#### e. Properties配置文件

-   `Properties` 是专门用来**读写配置文件**（`.properties`），内部是键值对（`String`-`String`），常用来存储配置信息

```java
load(InputStream in) -> 将流中的数据加载到Properties集合中
    
创建配置文件:
  1.在模块下右键 -> file -> 取名为xxx.properties
  2.在xxx.properties文件中写配置数据
    a.key和value都是key=value形式
    b.key和value都是String的,但是不要加双引号
    c.每个键值对写完之后,需要换行再写下一对
    d.键值对之间最好不要有空格(空格可以有,但是不建议写)
    e.键值对中建议不要使用中文(中文可以有,但是直接读取会乱码,需要转换流转码)  
```

```java
url=jdbc:mysql://localhost:3306/test
username=root
password=123456
```

```java
Properties prop = new Properties();
try (FileReader fr = new FileReader("db.properties")) {
    prop.load(fr); // 读取文件到集合
}

String url = prop.getProperty("url");
String user = prop.getProperty("username");
String pwd = prop.getProperty("password");
System.out.println(url + " / " + user + " / " + pwd);

```

#### f. 序列流

```java
ObjectOutputStream / ObjectInputStream
把对象直接变成字节数据保存/传输，下次还能还原回对象
    
保存对象到文件（比如游戏存档、用户状态）
通过网络传输对象（Socket 传输 Java 对象）
缓存对象（下次启动直接读取，不用重新计算）
```

#### g. 打印流

```java
 1.构造:
   PrintStream(String fileName)
 2.方法:
   a.println(): 原样输出,自带换行效果
   b.print():  原样输出,不带换行效果    
 3.改变流向
   static void setOut(PrintStream out) -> 改变流向 ->让输出语句从控制台输出转移到指定文件中
```

```java
PrintStream ps = new PrintStream("log.txt");
System.setOut(ps); // 改变标准输出到文件

System.out.println("小馒头上线啦！");
System.out.println("今天学习了打印流～");
ps.close();  //可以将输出的内容以及详细信息放到日志文件中,永久保存
```

#### h. Commons-io工具包

```java
1.Apache开发
2.jar包
    IOUtils类
- 静态方法：IOUtils.copy(InputStream in,OutputStream out)传递字节流，实现文件复制。
- 静态方法：IOUtils.closeQuietly(任意流对象)悄悄的释放资源，自动处理close()方法抛出的异常。
    FileUtils类
- 静态方法：FileUtils.copyDirectoryToDirectory(File src,File dest);
           传递File类型的目录，进行整个目录的复制，自动进行递归遍历。
           
           参数:
             src:要复制的文件夹路径
             dest:要将文件夹粘贴到哪里去
             
- 静态方法：writeStringToFile(File file,String str)写字符串到文本文件中。
- 静态方法：String readFileToString(File file)读取文本文件，返回字符串。
```

![image-20250815213550587](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20250815213550587.png)

### 14. jdk新特性

#### a. 正则表达式

```java
 String类中和正则表达式相关的方法
        boolean matches(String regex) 判断字符串是否匹配给定的正则表达式。
        String[] split(String regex) 根据给定正则表达式的匹配拆分此字符串。
        String replaceAll(String regex, String replacement)把满足正则表达式的字符串,替换为新的字符
```

#### b. Lombok

-   简化 JavaBean

#### c. 单例模式

-   让**某个类在整个进程中只有一个对象**，并且**全局都能拿到这同一个对象**

#### d. 函数式接口

```java
1.函数式接口:
  有且只有一个抽象方法的接口
2.检测:
  @FunctionalInterface
```

-   **Supplier<T>**：🧰 供给者/工厂 —— “我不吃输入，只**产出 T**”。`T get()`

-   **Consumer<T>**：🍽️ 消费者/终点 —— “我**吃 T**，但**不产出**”。`void accept(T t)`

-   **Function<T,R>**：🪄 变换器/映射器 —— “把 **T 变 R**”。`R apply(T t)`

-   **Predicate<T>**：🚦 守门员/筛选器 —— “对 **T 判断真假**”。`boolean test(T t)`

>   **Stream** 里`filter` 用 **Predicate**，`map` 用 **Function**，`forEach` 用 **Consumer**，`generate` 用 **Supplier**。

#### e. Stream流

-   核心思想: **把对集合的操作（过滤、排序、映射、聚合...）用链式调用的方式表达出来**，而不是自己写 for 循环。
-   常用方法
    -   获取流：`stream()`
    -   中间操作：`filter`、`map`、`sorted`、`distinct`
    -   终结操作：`forEach`、`collect`、`count`

```java
获取Stream流:
1.针对集合:Collection中的方法
    Stream<E> stream()  
    
2.针对数组:Stream接口中的静态方法:
    static <T> Stream<T> of(T... values)  

三步走:
1.获取流（从集合/数组创建）
list.stream()
2.中间操作（可以链式多个，比如过滤、排序、映射）
filter、map、sorted...
3.终结操作（触发执行，比如 forEach、collect、count）
```


**1.获取流**


```
List<String> list = Arrays.asList("张三", "李四", "王五");
Stream<String> stream = list.stream(); // 获取流
```

**2.中间操作**

-   **filter** 过滤

```
list.stream()
    .filter(name -> name.startsWith("张"))
    .forEach(System.out::println);
// 输出: 张三
```

-   **map** 转换数据（比如取长度）

```
list.stream()
    .map(String::length)
    .forEach(System.out::println);
// 输出: 2 2 2
```

-   **sorted** 排序

```
list.stream()
    .sorted()
    .forEach(System.out::println);
// 输出: 李四 王五 张三 (按字典序)
```

-   **distinct** 去重

```
Arrays.asList(1,2,2,3,3,4).stream()
    .distinct()
    .forEach(System.out::println);
// 输出: 1 2 3 4
```

**3.终结操作**

-   **forEach** 遍历

```
list.stream().forEach(System.out::println);
```

-   **count** 统计个数

```
long c = list.stream().count();
System.out.println(c); // 3
```

-   **collect** 收集到集合

```
List<String> result = list.stream()
    .filter(s -> s.contains("张"))
    .collect(Collectors.toList());
System.out.println(result); // [张三]
```

#### f. 方法引用

-   **当 Lambda 只是调用一个已有方法时，就用 `::` 方法引用来替代。**

```java
例子
    System.out::println
	String::toUpperCase / String::equals
	Integer::parseInt
	类名::new（构造器引用，配合 Supplier/Function 很常见）
```

#### g. Junit单元测试

```java
1.概述:Junit是一个单元测试框架,可以代替main方法去执行其他的方法
2.作用:可以单独执行一个方法,测试该方法是否能跑通
3.注意:Junit是第三方工具,所以使用之前需要导入jar包    
```

```java
1.导入Junit的jar包
2.定义一个方法,在方法上写注解: @Test
3.执行方法:
  a.点击该方法左边的绿色按钮,点击run执行 -> 单独执行一个指定的方法
  b.如果想要执行所有带@Test的方法,点击类名左边绿色按钮,点击run执行 -> 执行当前类中所有带@Test的方法   
```

### 15. 注解与枚举

#### a. 注解

```java
JDK中的注解:
        @Override  ->  检测此方法是否为重写方法
        @Deprecated -> 方法已经过时,不推荐使用
                       调用方法的时候,方法上会有横线,但是能用
        @SuppressWarnings->消除警告  @SuppressWarnings("all")  
```

-   自定义注解： 框架使用

#### b. 枚举

-   一组有限且固定的常量的集合，大写

```java
public enum Day {
    MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY, SUNDAY
}

public class Demo {
    public static void main(String[] args) {
        // 1. 定义一个枚举变量
        Day today = Day.MONDAY;

        // 2. 打印枚举
        System.out.println(today); // 输出：MONDAY

        // 3. 用 == 来比较（推荐）
        if (today == Day.MONDAY) {
            System.out.println("周一上班，馒头加油 💪");
        }

        // 4. switch 语句
        switch (today) {
            case MONDAY:
                System.out.println("开工大吉 🎉");
                break;
            case SATURDAY:
                System.out.println("周末快乐 😎");
                break;
        }

        // 5. 遍历所有枚举值
        for (Day d : Day.values()) {
            System.out.println(d);
        }
    }
}
```

| 方法名            | 说明               |
| ----------------- | ------------------ |
| String toString() | 返回枚举值的名字   |
| values()          | 返回所有与的枚举值 |

-   属性枚举 : 属性 + 构造方法

    ```java
    enum OrderStatus {
        NEW(1, "新建"),
        PAID(2, "已支付"),
        SHIPPED(3, "已发货"),
        COMPLETED(4, "已完成");
    
        private int code;       // 状态码
        private String desc;    // 状态描述
    
        // 构造方法必须是 private（默认就是）
        OrderStatus(int code, String desc) {
            this.code = code;
            this.desc = desc;
        }
    
        // Getter 方法
        public int getCode() { return code; }
        public String getDesc() { return desc; }
    }
    ```

### 16. 反射

-   在 **程序运行时** 动态地获取类的信息，并能操作它（创建对象、调用方法、修改属性）

```java
1.获取类的信息（类名、属性、方法、构造方法）。
2.动态创建对象（不用写死 new）。
3.调用对象的方法（即使方法名是字符串）。
4.修改属性值（包括 private 的）。
5.框架的核心基础（Spring、MyBatis、JUnit 都依赖反射）。
```

-   获取class对象的三种形式

```java
// 方式1：通过对象
Class c1 = obj.getClass();

// 方式2：通过类名
Class c2 = Person.class;

// 方式3：通过全类名字符串（最常用）
Class c3 = Class.forName("com.demo.Person");
```

| 分类             | 方法                                              | 作用                          | 重要程度  |
| ---------------- | ------------------------------------------------- | ----------------------------- | --------- |
| **操作类**       | `getName()`                                       | 获取类的全限定名（包名+类名） | 了解      |
|                  | `getSimpleName()`                                 | 获取类名（不带包名）          | 了解      |
|                  | `getFields()` / `getDeclaredFields()`             | 获取类的成员变量（公共/所有） | 了解      |
|                  | `getMethods()` / `getDeclaredMethods()`           | 获取类的方法（公共/所有）     | 了解      |
|                  | `getConstructors()` / `getDeclaredConstructors()` | 获取类的构造方法（公共/所有） | 了解      |
| **操作构造方法** | `getConstructor(...)`                             | 获取指定参数的构造方法        | 重要      |
|                  | `newInstance(...)`                                | 通过构造方法创建对象          | 重要      |
| **操作方法**     | `getMethod(...)`                                  | 获取指定的方法对象            | 重要      |
|                  | `invoke(对象, 参数...)`                           | 执行方法                      | 核心 ⭐    |
| **操作成员变量** | `getDeclaredField(...)`                           | 获取指定字段对象              | 重要      |
|                  | `setAccessible(true)`                             | 破坏封装（访问 private 成员） | 了解/常见 |
|                  | `set(对象, 值)`                                   | 给字段赋值                    | 重要      |
|                  | `get(对象)`                                       | 获取字段值                    | 重要      |

# Git 版本控制(25/8/16-25/8/23)

### 1. 快捷键

```
T : 定位repository的文件搜索栏
? : 快捷键速查表
. : 打开网页版vscode
/ : 打开search
```

### 2. license 开源许可证

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20250822200120383.png" alt="image-20250822200120383" style="zoom: 33%;" />

-   GPL系 : 其他人修改后必须保持开源
-   MIT/BSD : 较为宽松,随意使用 

### 3. Git branch

-   main/master 主干分支
-   feature 特性分支,开发后merge进主分支
-   合并可以代码审核 code review

### 4. repository其他功能

#### a. Wiki

-   维基百科

#### b. Insight

-   一些项目活动信息的记录

####  c. Projects

-   项目看板 : 可视化管理工具
-   待办事项,已办事项,分配任务等

#### d. Actions

-   自动化工具

-   相关术语

    <img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20250822204105325.png" alt="image-20250822204105325" style="zoom: 50%;" />

-   CI/CD自动化流水线

    <img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20250822204331641.png" alt="image-20250822204331641" style="zoom: 33%;" />

### 5. Git分区

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20250822201605224.png" alt="image-20250822201605224" style="zoom: 33%;" />

### 6. 高级操作

-   Discard change: 放弃更改

-   stash : 暂时存储代码改动,方便切换其他分支

    <img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20250822201928195.png" alt="image-20250822201928195" style="zoom: 50%;" />

-   Reset : 重置, 把当前commit的history消除掉,如果已经上传远端,则必须强制推送

-   Revert : 反向操作,抵消上一次commit

-   Amend : 修改, commit后还可以改动

    <img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20250822202230760.png" alt="image-20250822202230760" style="zoom:50%;" />

-   cherry-pick : 任意选择多个commit 一起合并到另一分支

-   tag : 打上标签,例如版本号的发布 `v1.0`

### 7. 合并操作

-   merge : 普通合并
-   squash : 将多个commit压缩成一个合并,干净
-   rebase : 变基 ,强制推送

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20250822202725416.png" alt="image-20250822202725416" style="zoom:50%;" />

-   分支冲突时需要解决

### 8. Github开源贡献

```
流程:
1. 提交issue
2. fork到本地
3. 创建feature
4. 增加完毕后,先s]ync fork同步代码,再合并
5. pull request
```

### 9. IDE

-   gitignore : 忽略的文件目录,不会上传到远端仓库

-   checkout : 检出,切换到此分支

-   rollback :  等同于discard

-   reset

    <img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20250822203427445.png" alt="image-20250822203427445" style="zoom: 67%;" />

    -   Mixed : 记录还保留在磁盘上
    -   Hard : 本地磁盘也删除

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20250822203530138.png" alt="image-20250822203530138" style="zoom:50%;" />

### 10. Git命令行

| 命令                    | 作用说明                   | 举例                        |
| ----------------------- | -------------------------- | --------------------------- |
| `git init`              | 初始化一个新的Git仓库      | `git init`                  |
| `git clone <url>`       | 克隆远程仓库到本地         | `git clone https://xxx.git` |
| `git status`            | 查看当前仓库状态           | `git status`                |
| `git add <文件>`        | 将文件添加到暂存区         | `git add main.py`           |
| `git add .`             | 添加所有修改到暂存区       | `git add .`                 |
| `git commit -m "说明"`  | 提交暂存区到本地仓库       | `git commit -m "初次提交"`  |
| `git log`               | 查看提交记录               | `git log`                   |
| `git diff`              | 查看未暂存的文件改动       | `git diff`                  |
| `git branch`            | 查看本地分支               | `git branch`                |
| `git branch <name>`     | 创建新分支                 | `git branch dev`            |
| `git checkout <name>`   | 切换分支                   | `git checkout dev`          |
| `git merge <name>`      | 合并指定分支到当前分支     | `git merge dev`             |
| `git pull`              | 拉取远程仓库最新代码并合并 | `git pull`                  |
| `git push`              | 推送本地代码到远程仓库     | `git push origin main`      |
| `git remote -v`         | 查看已连接的远程仓库地址   | `git remote -v`             |
| `git reset --hard <id>` | 回退到指定版本             | `git reset --hard a1b2c3`   |

```
常用组合:
1. git add . → git commit -m "说明" → git push
2. git status → git log状态 → 日志
3. git checkout -b 分支名
```

-   gitLFS : 大文件系统,可以上传大文件

### 11. Git 配置

```
git config --global user.name "你的名字"      # 配置用户名
git config --global user.email "你的邮箱"    # 配置邮箱
git config --list                            # 查看当前配置
```

# Linux操作系统(25/8/24-25/9/10)

## **1. 导航目录**

### **(1) `cd` — Change Directory**

-   **含义**：切换当前所在目录。

-   **用法**：

    ```
    cd /home/user/Documents  # 进入指定目录
    cd ..                    # 返回上一级目录
    cd                       # 回到用户主目录
    ```

-   **缩写**：`c`hange `d`irectory。

------

### **(2) `pwd` — Print Working Directory**

-   **含义**：显示当前所在目录的绝对路径。

-   **用法**：

    ```
    pwd
    # 输出: /home/user/Documents
    ```

-   **缩写**：`p`rint `w`orking `d`irectory。

------

## **2. 创建文件和目录**

### **(3) `touch`**

-   **含义**：创建空文件，或更新文件的时间戳（不改变内容）。

-   **用法**：

    ```
    touch test.txt          # 创建一个名为test.txt的空文件
    ```

-   **缩写**：无严格缩写含义，意思是“触碰文件”（更新其状态）。

------

### **(4) `mkdir` — Make Directory**

-   **含义**：创建目录。

-   **用法**：

    ```
    mkdir myfolder          # 创建一个名为myfolder的目录
    mkdir -p a/b/c          # 递归创建目录
    ```

-   **缩写**：`m`ake `dir`ectory。

------

## **3. 查看目录内容**

### **(5) `ls` — List**

-   **含义**：列出目录下的文件和子目录。

-   **常用选项**：

    -   `-l`：长格式（显示详细信息：权限、大小、修改时间）
    -   `-a`：显示所有文件（包括隐藏文件）
    -   `-h`：以人类可读方式显示文件大小

-   **用法**：

    ```
    ls          # 列出当前目录内容
    ls -la      # 列出所有文件及详细信息
    ```

-   **缩写**：`l`i`s`t。

------

## **4. 文件与目录操作**

### **(6) `cp` — Copy**

-   **含义**：复制文件或目录。

-   **用法**：

    ```
    cp file1.txt file2.txt      # 复制文件
    cp -r dir1 dir2             # 递归复制目录
    ```

-   **缩写**：`c`o`p`y。

------

### **(7) `mv` — Move**

-   **含义**：移动或重命名文件/目录。

-   **用法**：

    ```
    mv oldname.txt newname.txt  # 重命名文件
    mv file.txt /tmp/           # 移动文件到/tmp目录
    ```

-   **缩写**：`m`o`v`e。

------

### **(8) `rm` — Remove**

-   **含义**：删除文件（危险操作！）。

-   **常用选项**：

    -   `-r`：递归删除目录及内容
    -   `-f`：强制删除，不提示

-   **用法**：

    ```
    rm file.txt
    rm -rf folder/
    ```

-   **缩写**：`r`e`m`ove。

------

### **(9) `rmdir` — Remove Directory**

-   **含义**：删除空目录（只能删除没有内容的目录）。

-   **用法**：

    ```
    rmdir emptyfolder
    ```

-   **缩写**：`r`e`m`ove `dir`ectory。

------

## **5. 输出与重定向**

### **(10) `echo`**

-   **含义**：在终端打印一行文本，常用于输出变量或写文件。

-   **用法**：

    ```
    echo "Hello World"
    echo "data" > file.txt      # 将"data"写入文件(覆盖)
    echo "more" >> file.txt     # 追加内容到文件
    ```

-   **缩写**：无缩写含义，字面意思“回声”（把内容回显）。

### **一、文件内容查看命令**

#### **1. `cat`** —— **concatenate（连接）**

-   **作用**：查看文件全部内容、合并多个文件、重定向输出。

-   **常用选项**：

    -   `cat file.txt`：显示文件内容。
    -   `cat -n file.txt`：显示文件并带行号。

-   **例子**：

    ```
    cat -n hello.txt
    ```

#### **2. `head`** —— **文件头部**

-   **作用**：查看文件开头的部分内容。
-   **常用选项**：
    -   `head file.txt`：默认显示前 10 行。
    -   `head -n 5 file.txt`：显示前 5 行。
    -   `head -c 20 file.txt`：显示前 20 个字节。

#### **3. `tail`** —— **文件尾部**

-   **作用**：查看文件结尾的部分内容。
-   **常用选项**：
    -   `tail file.txt`：默认显示最后 10 行。
    -   `tail -n 5 file.txt`：显示最后 5 行。
    -   `tail -f file.txt`：动态实时查看文件更新（常用来监控日志）。

#### **4. `diff`** —— **difference（差异）**

-   **作用**：比较两个文件内容的不同。
-   **常用选项**：
    -   `diff file1.txt file2.txt`
    -   `diff -r dir1 dir2`：递归比较两个目录。
-   **用途**：代码版本对比、配置文件检查。

------

### **二、文件创建与权限管理**

#### **5. `touch`** —— **触碰（更新时间戳）**

-   **作用**：

    -   创建一个空文件。
    -   更新文件的最后修改时间。

-   **例子**：

    ```
    touch newfile.txt
    ```

#### **6. `chown`** —— **change owner（更改所有者）**

-   **作用**：更改文件/目录的拥有者和所属组。
-   **常用选项**：
    -   `chown user file`
    -   `chown -R user:group dir`（递归修改目录及内容）

#### **7. `chmod`** —— **change mode（更改模式）**

-   **作用**：修改文件/目录权限。

-   **两种表示法**：

    -   **数字**：`chmod 755 file`
        -   `7` = rwx (可读/写/执行)
        -   `5` = r-x
    -   **符号**：`chmod u+x file`
        -   `u` (user/所有者), `g` (group/组), `o` (others/其他)

-   **例子**：

    ```
    chmod 644 file.txt  # 所有者可读写，其他人只能读
    ```

------

### **三、用户管理**

#### **8. `useradd`** —— **添加用户**

-   **作用**：创建新用户账户。

-   **例子**：

    ```
    sudo useradd username
    ```

#### **9. `passwd`** —— **password（密码）**

-   **作用**：为用户设置或修改密码。

-   **例子**：

    ```
    sudo passwd username
    ```

#### **10. `usermod`** —— **user modify（修改用户）**

-   **作用**：修改用户属性。

-   **例子**：

    ```
    sudo usermod -d /new/home username  # 修改主目录
    sudo usermod -s /bin/bash username  # 修改默认shell
    ```

#### **11. `groupadd` / `usermod -aG`**

-   **作用**：添加用户到组。

-   **例子**：

    ```
    sudo groupadd dev
    sudo usermod -aG dev username
    ```

#### **12. `passwd -l/-u`** —— **锁定 / 解锁用户**

-   `-l` = lock
-   `-u` = unlock

#### **13. `userdel`** —— **删除用户**

-   **作用**：删除用户账户。

-   **例子**：

    ```
    sudo userdel -r username  # 同时删除主目录
    ```

# MySQL (基础25/9/20-25/926,进阶)

# HTML5 (25/9/15-25/9/20)

# CSS3 (25/10/10-25/10/21)

##  一、编写位置

### 1. 行内样式

```
写在style属性中
```

![image-20251011220346855](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251011220346855.png)

### 2. 内部样式

```
单独放在<style>标签中
```

![image-20251011220428165](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251011220428165.png)

### 3. 外部样式

```
引入
```

![image-20251011220455789](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251011220455789.png)

### 4. 优先级

-   行内> 内部=外部 (后来者居上)

## 二、语法

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251011220635501.png" alt="image-20251011220635501" style="zoom:67%;" />

### 1. 选择器

-   通配选择器 : 选中HTML所有元素

```
* {
	属性名:属性值;
}
```

-   元素选择器 : 某种元素设置样式

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251011221008240.png" alt="image-20251011221008240" style="zoom:67%;" />

-   类选择器

    ![image-20251011221054335](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251011221054335.png)

-   ID选择器 : 一个元素只有唯一id

![image-20251011221123727](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251011221123727.png)

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251011221150549.png" alt="image-20251011221150549" style="zoom:67%;" />

### 2. 复合选择器

#### a. 交集选择器

-   且的关系

![image-20251011221417601](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251011221417601.png)

#### b. 并集选择器

-   或的关系,逗号隔开

![image-20251011221508322](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251011221508322.png)

#### c. 元素间关系

```
父元素
子元素
祖先元素
后代元素
兄弟元素
```

#### d. 后代选择器

-   指定符合要求的后代元素,用空格隔开

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251011221724231.png" alt="image-20251011221724231" style="zoom:67%;" />

#### e. 子代选择器

-   子(儿子)元素,用`>`隔开

![image-20251011221828025](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251011221828025.png)

#### f. 兄弟选择器

-   相邻兄弟选择器,用`+`,相邻就是紧挨着的下一个

-   通用兄弟选择器,所有兄弟,用`~`

#### g. 属性选择器

![image-20251011222030982](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251011222030982.png)

#### h. 伪类选择器

##### 1) 动态伪类

-   **lvha**

![image-20251011222240724](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251011222240724.png)

##### 2) 结构伪类

![image-20251011222331798](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251011222331798.png)

##### 3) 否定伪类

![image-20251011222412853](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251011222412853.png)

#### i. 伪元素选择器

-   选取**元素**中的特殊位置

![image-20251011222553163](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251011222553163.png)

#### j. 选择器优先级

![image-20251011224558164](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251011224558164.png)

### 3. css三大特性

-   层叠性: 就是样式冲突
-   继承性: 元素自动拥有最近的父元素的一些样式
-   优先级

### 4. css常用属性

#### a. 像素px

#### b. 颜色

-   颜色名
-   rgb/rbga : 即红绿蓝,和透明度

-   HEX/HEXA :  #rrggbb

-   HSL/HSLA

#### c. 字体

-   `font-size`

-   字体族 : `font-family` 设置字体类型

-   字体风格 : `font-style` : normal/italic
    -   字体粗细`font-weight`

#### d.文本

-   文本颜色`color`
-   字母间距: `letter-spacing`  单词间距: `word-spacing`

-   文本修饰 : `text-decoration`

-   文本缩进 : `text-indent` 用em
-   文本对齐 : `text-align`
-   行高 : `line-height`

#### e. 各种列表,表格等属性

## 三、盒子模型

-   长度单位

    ![image-20251014212319917](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251014212319917.png)

-   块级元素block独占一行,行内元素inline不独占一行

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251014212353622.png" alt="image-20251014212353622" style="zoom: 67%;" />

-   修改元素模型 : `display`

-   盒子模型组成 : margin, border , padding , content

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251022143119277.png" alt="image-20251022143119277" style="zoom:67%;" />

-   盒子模型组成的各个参数

## 四、定位

#### a. 相对定位

-   设置 position : relation
-   相对自己原来的位置,配合绝对定位使用

#### b. 绝对定位

-   position : absolute

-   参考其**包含快**

#### c. 固定定位

-   position: fixed
-   参考其**视口**

#### d. 粘性定位

-   position : sticky
-   常用top值

#### e. 定位层级

-   属性`z-index `调整

## 五、css3新增属性

## 六、伸缩盒模型

-   也就是`flex`布局

-   设置`display: flex`

-   主轴与侧轴 : 左到右, 上到下

-   各自属性名 : 轴的方向,按行方式,对齐方式
-   伸缩性

## 七、响应式布局

-   媒体查询

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251022144141538.png" alt="image-20251022144141538" style="zoom:67%;" />

-   运算符

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251022144214749.png" alt="image-20251022144214749" style="zoom:67%;" />

## 八、BFC

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251022144310789.png" alt="image-20251022144310789" style="zoom:67%;" />

# JavaScript (25/10/22-25/11/1)

## 一、基础语法

-   变量命名用 `let`, `var`

-   常量命名用`const`

-   输入输出

    ```javascript
    /* 输出 */
    document.write('xxx')
    alert('xxx')
    console.log('xxx')
    /* 输入 */
    prompt('xxx')
    let x = +prompt('xxx')  // 字符串转为数值
    ```

-   数组 

    ```javascript
    let arr = [1,2,3]
    let a = arr.length
    
    /* 数组增删改查 */
    arr.push(xx)  //新增到末尾
    arr.unshift(xx) //新增到开头
    arr.pop()  //删除末尾
    arr.shift()  // 删除第一个
    arr.spolice(start,deleteCount)
    ```

-   函数

    ```javascript
    function xx() {
        
    }
    
    // 匿名函数
    function() {}
    
    //短路逻辑
    &&和||
    A || B：取第一个真值。
    A && B：若 A 为真，返回 B；若 A 为假，返回 A。
    ```

-   对象

```JavaScript
let 对象名 = {} //声明
//属性,方法,打点调用,字符串打[]
for in //遍历对象
    
/* 内置对象 */
Math.random()  //随机数[0,1)
```

## 二、 Web-APIs

### 1. DOM 对象

-   `const` 优先,地址不变就用它

-   DOM 文档对象模型,操作网页内容

    ```JavaScript
    /* 根据css选择器获取dom元素 */
    document.querySelector('xxx')   //匹配第一个元素
    document.querySelectorAll('xxx')   //匹配多个元素, 返回 NodeList 对象集合 , 也就是伪数组,只能遍历修改元素
    
    /* 操作元素内容 */
    const info = document.querySelector('.info')
    info.innerText = 'xxx'           //innerText 不解析标签,纯文本
    info.innerHTML = '<br>xxx</br>'  //innerHTML 解析标签
    
    /* 操作元素属性 */
    const info = document.querySelector('img')
    pic.src = './images/01.png'
    pic.title = 'xxxx'
    
    /* 操作元素样式属性 */
    const info = document.querySelector('.box')
    box.style.width = '200px'    // style
    元素.className = 'active'   // className 增加类名
    /* classList */
    box.classList.add('类名')     //追加一个类
    box.classList.remove('类名')     //删除一个类
    box.classList.toggle('类名')     //切换一个类
    
    /* 表单 */
    
    /* 自定义属性 */
    1. 标签上以data-* 格式开头
    2. BOM上以dataset 对象方式获取
    
    /* 定时器 */
    let x = setInterval(函数名,间隔时间(ms))  //开启定时器
    clearInterval(x)  //关闭定时器
    ```

### 2. DOM事件

-   事件监听

```javascript
/* 点击按钮,弹出对话框 */
const btn = document.querySelector('button')
btn.addEventListener('click',function () {
    alert('hello button')
})
```

-   事件类型

    <img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251023144803262.png" alt="image-20251023144803262" style="zoom:67%;" />

-   事件对象 : event , e

```JavaScript
const input = document.querySelector('input')
input.addEventListener('keyup',function (e) {   //e就是事件对象
    if(e.key === 'Enter')
      console.log('click enter')
})
```

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251023150344631.png" alt="image-20251023150344631" style="zoom: 50%;" />

-   环境对象 : this 

    >   谁调用,this就指向谁

-   回调函数 : 函数作为参数传递给另一个函数, 该函数就是回调函数

### 3. 事件流

![image-20251030085553143](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251030085553143.png)

-   事件捕获 : 从根元素到里 `DOM.addEventListerer()`

-   **事件冒泡** : 当一个元素的事件被触发时，同样的事件将会在该元素的所有祖先元素中依次被触发,默认存在

-   阻止冒泡: `事件对象.stopPropagation()`
    -   阻止元素默认行为 : `e.preventDefault()`, 如表单的跳转

-   解绑事件 

    -   on事件方式, 直接 null覆盖

    -   addEventListener方式 : `removeEventListener(事件类型,函数)`

-   事件委托 : 给父元素注册事件,触发子元素,会冒泡到子元素身上
    -   实现: 事件对象.target.tagName

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251030091234282.png" alt="image-20251030091234282" style="zoom:67%;" />

### 4. 日期

```javascript
/* 获取当前/指定时间 */
const date = new Date()
const date = new Date('2025-10-30')

/* 时间戳 */
1970 01 01 00 00 起到现在的毫秒数
date.getTime()
+new Date()
Date.now()
```

### 5. BOM对象

-   浏览器对象, 基于window

-   定时器延时

    ```javascript
    let timer = setTimeout(回调函数,ms)
    clearTimeout(timer)
    ```

### 6. 本地存储

-   localStorage , 以键值对形式存储

```javascript
localStorage.setItem(key,value)
localStorage.getItem(key)
localStorage.removeItem(key)
```

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251030112928392.png" alt="image-20251030112928392" style="zoom:67%;" />

### 7. 正则表达式

```javascript
const a = /xxx/      //定义
a.test(str) 		 //判断(返回true/false)
a.exec(str) 		 //检索(返回数组,否则null)
str.replace(/xxx/,'替换的文本')  //替换
```

## 三、JS进阶

### 1. 作用域 

-   局部和全局

-   作用域链 : 本质上是底层的变量查找机制, 也就是逐级查找父级作用域直到全局

-   闭包 : 内层函数 + 外层函数的变量  , 使其外部可以访问函数内部变量不会丢

```js
function createCounter() {
  let count = 0;      // 外部变量

  return function () {  // 内部函数
    count++;
    console.log(count);
  };
}
const counter = createCounter(); // 这里执行了一次外层函数
counter(); // 1
counter(); // 2
counter(); // 3
```

-   变量/函数提升 : var, 函数或变量在声明前就调用

### 2. 函数参数

```js
1. 动态参数 arguments,是一个伪数组,只存在函数中
2. 剩余参数 ...arr , 写在末尾形参,真数组
```

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251031110922977.png" alt="image-20251031110922977" style="zoom:67%;" />

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251031111020069.png" alt="image-20251031111020069" style="zoom:67%;" />

-   箭头函数

```js
// 普通
function add(a, b) {
  return a + b;
}
// 箭头
const add = (a, b) => a + b;

1.只有一个参数时省略()
2.函数体只有一行代码省略{},并自动返回
3.加括号的函数体返回对象表达式
4.可以用剩余次数
```

-   this 会从作用域链的上一层沿用this ,而不会是谁调用用谁

```js
function show() {
  console.log(this);
}
const obj = { show };
obj.show();      // this === obj
show();          // this === window

const obj = {
  name: '馒头',
  init() {
    // 这里是普通函数，this === obj
    setTimeout(() => {
      // 这里是箭头函数，this 用外面那一层的（也就是 obj）
      console.log(this.name);
    }, 1000);
  }
};
obj.init(); // 1 秒后输出：馒头
```

### 3. 解构赋值

-   **数组解构** 

    ```js
    const [a,b,c] = [1,2,3]
    ;[b,c] = [c, b]
    ;(function t() {})()
    
    立即执行函数和数组解构前必须加分号
    ```

-   **对象解构**

```js
1.基本用法
const user = {
  name: '小馒头',
  age: 22,
  school: 'XDU'
};
const { name, age } = user;
console.log(name); // '小馒头'
console.log(age);  // 22

2.改名字
const user = {
    username: 'mantou', 
    id: 1001 
};
const { username: name, id: userId } = user;
console.log(name);   // 'mantou'
console.log(userId); // 1001

```

-   **遍历数组forEach**

    <img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251031120033475.png" alt="image-20251031120033475" style="zoom:67%;" />

    ```js
    const arr = ['red','green','blue']
    arr.forEach(function (item, index){
        console.log(${item})
    	console.log(${index})
    })
    ```

### 4. 内置构造函数

```js
1. object,常用静态方法如下
Object.keys(xxx)   //返回键
Object.values(xxx)  //返回值

2.array
arr.filter  //过滤
arr.map    //迭代
arr.reduce  //累加

3.string
4.number
```

### 5. 拷贝

-   **浅拷贝** / **深拷贝** : 只针对引用类型
-   浅拷贝拷贝的是地址  `Object.assign()`

### 6. 异常

```js
1. throw
2. try...catch  finally
```

### 7. this指向

```js
改变this指向
1. fun.call(thisArg,arg1,arg2,...)  //用的少
2. fun.apply(thisArg,[argsArray])    //比如使用Math.max()求数组最大值
3. fun.bind(thisArg,arg1,arg2,...)   //不调用函数,只改变指向
```

### 8. 性能优化

-   **防抖**: 频繁触发事件,只执行最后一次
-   **节流**: 频繁触发事件,但只再nms中执行一次函数

## 四、浏览器开发者工具

# Axios+SSE+ES模块化+Node(npm) (2025/11/3-25/11/6)

## 一、AJax (axios)

### 1. 浏览器基础

-   **URL**: 统一资源定位符,也就是网址

    ```ruby
    1.协议://域名:端口/路径?查询参数#锚点
    2.https://api.example.com:8080/user/info?id=7#top
    
    协议：https / http / ws / wss
    域名：服务器地址（可用 IP）
    端口：HTTP 默认 80，HTTPS 默认 443
    路径：接口资源地址
    查询参数：?key=value&key2=value2
    锚点（#）：前端页面内定位，不会发给服务器
    ```

-   **HTTP 请求方法**

| 方法     | 作用                 | 常见场景         |
| -------- | -------------------- | ---------------- |
| **GET**  | 获取数据             | 查询、列表       |
| **POST** | 提交数据             | 登录、注册、上传 |
| PUT      | 修改数据（整体替换） | 编辑记录         |
| PATCH    | 局部修改数据         | 局部更新         |
| DELETE   | 删除数据             | 删除记录         |

-   **HTTP 请求报文(Request)**

```ruby
浏览器发给服务器:
请求行：GET /user/info HTTP/1.1
请求头：Header（说明信息，如类型、token）
请求体：Body（POST/PUT 才有，一般是 JSON）
Content-Type: application/json
Authorization: Bearer xxxxxx
Accept: application/json

```

-   **HTTP 响应报文（Response）**

```ruby
状态行：HTTP/1.1 200 OK
响应头：Content-Type、Set-Cookie、Cache-Control...
响应体：真正的数据内容（通常是 JSON）
```

| 状态码 | 含义       | 例子          |
| ------ | ---------- | ------------- |
| 200    | 成功       | 请求成功      |
| 201    | 创建成功   | POST 新建资源 |
| 400    | 客户端错误 | 参数不对      |
| 401    | 未授权     | Token 无效    |
| 404    | 资源不存在 | 找不到接口    |
| 500    | 服务器错误 | 后端异常      |

### 2. **axios基本用法**

```js
/* get请求 */
axios.get('/api/user', {
  params: { id: 7 }   // 自动拼接成浏览器这种 ?id=7 ,查询参数,params 参数名:值
  method: '请求方法'
  data:{参数名:值}  //提交数据
}).then(res => {
  console.log(res.data);
}).catch(err => {
  console.error(err);
});
/* post请求 */
axios.post('/api/login', {
  username: 'mantou',
  password: '123456'
}).then(res => {
  console.log('登录成功', res.data);
}).catch(err => {
  console.error('登录失败', err);
});
/* 错误处理 */
try {
  const res = await axios.get('/api/xx');
} catch (error) {
  if (error.response) { 
    console.error('后端错误：', error.response.status);  //状态码  error.response.data  后端返回内容
  } else {
    console.error('网络异常');
  }
}
```

-   **图片上传FormData**

    ```ruby
    const fd = new FormData()
    fd.append(参数名,值)
    ```

    ```js
    const fd = new FormData();
    fd.append('file', fileInput.files[0]);   // <input type="file" />
    const res = await axios.post('/api/upload', fd, {
      headers: { /* axios 会自动带 multipart，无需手写 Content-Type */ },
      onUploadProgress: (e) => {
        const percent = Math.round((e.loaded / e.total) * 100);
        console.log('进度', percent + '%');
      }
    });
    ```


### 3. Promise基础

-   **三个状态**：`pending → fulfilled`（成功）或 `rejected`（失败），**不可逆**。

```js
const p = new Promise((resolve, reject) => {
  // 异步操作...
  if (ok) resolve(result);
  else reject(new Error('失败原因'));
});
p.then(v => {...}).catch(err => {...}).finally(() => {...});
```

-   **链式调用**
    -   **场景**：上一个请求的结果是下一个请求的入参（级联请求）

```js
axios.get('/api/province')
  .then(res => axios.get('/api/city', { params: { pname: res.data.list[0] }}))
  .then(res => axios.get('/api/area', { params: { cname: res.data.list[0] }}))
  .then(res => console.log('area', res.data))
  .catch(err => console.error('链式失败', err));
```

### 4. async / await

```js
async function loadCascaded() {
  try {
    const p = await axios.get('/api/province');
    const pname = p.data.list[0];

    const c = await axios.get('/api/city', { params: { pname }});
    const cname = c.data.list[0];

    const a = await axios.get('/api/area', { params: { pname, cname }});
    console.log(a.data.list[0]);
  } catch (e) {
    console.error('失败', e);
  } finally {
    // 收尾：loading 置 false 等
  }
}

//优化,循环里 await 会串行且慢：用 map + Promise.all
const results = await Promise.all(ids.map(id => axios.get(`/api/item/${id}`)));
```

### 5. Promise.all并发

-   `Promise.all([p1,p2])`：**全部成功才成功**；有一个 reject 就抛错。

-   `Promise.allSettled([p1,p2])`：总是成功，结果含 `status: 'fulfilled'|'rejected'`。

-   `Promise.race([p1,timeout])`：谁先完成用谁（常做**超时控制**）。

-   `Promise.any([p1,p2])`：任意一个成功就成功（全失败才抛 `AggregateError`）

## 二、Node.js

### 1.Node 概念

-   用 **V8 引擎** 在浏览器外面跑 JavaScript 的环境。
-   没有 DOM、alert、window，这些只在浏览器里有。
-   有一堆内置模块：`fs`、`path`、`http` 等，适合做脚本和服务端
-   运行: `node xxx.js`

-   **path模块**: 路径处理

```js
const path = require('path')
path.join(__dirname, 'public', 'index.html');  // 拼路径
path.resolve(__dirname, 'dist');               // 变成绝对路径
__dirname：当前文件所在目录的绝对路径
path.join 自动处理 /、\\
```

### 2.端口与http

```text
http://localhost:5173/path?key=1
协议    主机      端口  路径 查询参数
```

```js
const http = require('http');
const server = http.createServer((req, res) => {
  // req：请求信息（url、method、headers…）
  // res：响应对象，用来回数据给客户端
  res.statusCode = 200;
  res.setHeader('Content-Type', 'text/plain; charset=utf-8');
  res.end('Hello, world!');
});

server.listen(3000, () => {
  console.log('Server running at http://localhost:3000');
});
```

-   `http://loclahost:3000` **本机域名打开端口**

### 3.模块化

#### a. CommonJS (传统模块)

-   **导出: module.exports = {}**
-   **导入: require('xxx')**,**引入模块**

```js
// a.js
const fs = require('fs');
function read() {}
module.exports = { read };

// b.js
const { read } = require('./a');
read();
```

#### b. ECMAScript (ES Module)

-   **导出: export default {}**
-   **导入: import 变量名 from '模块名/路径'**

```js
// api.js  命名导出
export const a = 1;
export function foo() {}
// api.js  默认导出
export default function request() {}
// 导入
import request, { a, foo as bar } from './api.js';
```

### 4. npm包管理

```bash
npm init -y      # 生成 package.json
npm i 软件包名称   # 生成 node_modeules 即包的源码
```

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251105155606386.png" alt="image-20251105155606386" style="zoom:80%;" />

```bash
node -v        # 查看 node 版本
npm -v         # 查看 npm 版本
cd 目录名       # 切换目录
cd ..          # 回到上一级
ls / dir       # 列出当前目录文件
mkdir xxx      # 创建文件夹
rm / del       # 删除
npm run dev    # 启动开发服务器
```

<img src="C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251105160803556.png" alt="image-20251105160803556" style="zoom:80%;" />

## 三、SSE

**SSE（Server-Sent Events）**：

-   协议层就是普通 HTTP,单向通信协议
-   响应头：`Content-Type: text/event-stream`
-   连接一旦建立，服务器可以不断往这条连接里 push 文本
-   前端用 `EventSource` 接收

## 四、CORS 跨域资源共享

-   也就是跨域问题 , 服务器通过响应头告诉浏览器“这台前端可以访问我

```ruby
Access-Control-Allow-Origin: http://localhost:5173
Access-Control-Allow-Methods: GET,POST,OPTIONS
Access-Control-Allow-Headers: Content-Type, Authorization
```

# Vue(2025/11/10-2025/11/27)

## 一、基础

```js
vue.js  //开发版本
vue.min.js //生产版本
Vue.config.productionTip = false  //阻止启动时的生产提示
```

```js
/* 创建Vue实例 */
const vm = new Vue({
    el:'root'       //为哪个容器服务
    data:{
    name: xxx
}
})
```

```js
/* Vue模板语法 */
1.插值语法:解析标签体内容
{{xxx}}
2.指令语法:解析标签
eg: v-bind:href="xxx"  简写 :href="xxx"

/* 数据绑定 */
1.单向绑定(v-bind)
2.双向绑定(v-model): 表单类元素(eg:input,selcet...)
	v-model:value 简写 v-model
```

```js
1.el两种写法
	.new Vue 配置el属性,创建实例时候就要配置
    用vm.$mount('xxx')  指定el值
2.data两种写法
	对象式,函数式(组件必须用这种)
```

-   **MVVM模型**
    -   M: 模型,data中的数据
    -   V: 视图,模板代码
    -   VM: 视图模型,vue实例

## 二、事件系统

```js
1.事件的基本使用
- 使用v-on:xxx 或 @xxx 绑定事件
- 事件回调配置在实例的methods对象中
2.事件修饰符
- prevent: 阻止默认事件
- stop:阻止事件冒泡
- once:事件只触发一次
3.键盘事件:keydown与keyup
- enter,delete,esc,space,tab(配合keydown),up,down,left,right(上下左右)
```

## 三、计算/绑定/监听

```js
//计算属性:computed,有缓存
computed:{
    fullname:{
        get(){
           return this.xxxx
        }
        set(){
            
        }
    }
}

- 没有set时,可以简写为
fullname(){
    
}
```

```js
//监视属性:watch,能异步
whatch:{
    immediate: true  //初始化时候 调用handler
    deep : true	     // 可以监测对象内部值改变(多层)
    handler(newValue,oldValue){
        
    }
}

- 没有时immediate,deep可以简写为
handler(){
    
}
```

 ```js
 //绑定样式
 1.class样式
 <div :class="xxx" </div>
 xxx : 值,数组,对象
 2.style样式
 <div :style="xxx" </div>
 xxx : 对象
 3.绑定的均在data里使用键值对写
 ```

## 四、条件,列表,表单

```js
//条件渲染
1..v-if,.v-else-if,.v-else
不展示的DOM元素直接被移除
2.v-show
只是使用样式隐藏掉
```

```js
//列表渲染
v-for:
语法: v-for="(item,index) in xxx" :key="xxx"
来遍历数组,对象

key的本质:key是虚拟DOM对象的标识,发生变化时,使用diff算法来更新
```

```js
收集表单数据:
<input type="text"/> 则v-model收集的是value值,用户输入就是value值
<input type="radio"/> 则v-model收集的是value值,且给标签配value值
<input type="checkbox"/> 则v-model收集的是布尔值(没配置value),配置数组后是数组
v-model的三个修饰符:
lazy:失去焦点再收集
number:输入字符串转为有效数字
trim:首位空格过滤
```

## 五、生命周期

-   **生命周期（Lifecycle）** = Vue 实例或组件 **从创建、挂载、更新到销毁的全过程。在不同阶段会自动触发一系列“生命周期钩子函数”（hooks）**

-   #### **三个主要阶段**

-   **挂载阶段（Mounting）**: Vue 实例开始建立 → 渲染到页面

| 阶段             | 发生了什么                                | 常用用途                                     |
| ---------------- | ----------------------------------------- | -------------------------------------------- |
| **beforeCreate** | 实例刚创建，`data`、`methods` 还没初始化  | 几乎不用                                     |
| **created**      | `data`、`methods` 已可用，但 DOM 还没渲染 | 适合发请求、初始化数据                       |
| **beforeMount**  | 模板编译好，还没挂载到页面                | 几乎不用                                     |
| **mounted**      | 页面渲染完成，DOM 可操作                  | **常用于操作 DOM、定时器、第三方插件初始化** |

   -   **更新阶段（Updating）**: 数据改变 → 触发视图重新渲染

| 阶段             | 发生了什么                    | 常用用途                 |
| ---------------- | ----------------------------- | ------------------------ |
| **beforeUpdate** | data 变了，但 DOM 还没更新    | 查看变化前的数据         |
| **updated**      | data 变了，DOM 也重新渲染完毕 | **适合操作更新后的 DOM** |
   -   **销毁阶段（Unmounting）**: 组件被移除或页面关闭时

| 阶段                     | 发生了什么             | 常用用途                 |
| ------------------------ | ---------------------- | ------------------------ |
| **beforeUnmount**        | 实例还活着，但即将销毁 | **清除定时器、取消监听** |
| **unmounted(destroyed)** | 实例彻底销毁           | 查看销毁后效果           |

```js
常用Hook函数:
1.mounted:发送ajax请求,启动定时器,绑定事件,订阅等初始化操作
2.beforeUnmount(beforeDestory):清楚定时器,解绑事件,取消订阅
3.created:发请求初始化
```

## 六、组件

**在代码里，一个组件一般包含:**

-   **模板 template**：这块组件的 HTML 结构

    **数据 data / `ref`**：这块组件自己维护的状态

    **方法 methods / 函数**：这块组件能执行的操作

    **props / emits**：和外部父组件通信的入口 & 出口

    -   结构写在 `<template>`

        逻辑写在 `<script setup>`

        样式写在 `<style>`

| 比较点   | （Vue2 + CDN）                      | （Vue3 + SFC）                            |
| -------- | ----------------------------------- | ----------------------------------------- |
| 入口     | `new Vue({ el: '#app' })`           | `createApp(App).mount('#app')`            |
| 定义组件 | `Vue.extend({ ... })`               | 一个 `.vue` 文件（`<template><script>`）  |
| 组件注册 | `Vue.component` / `components` 选项 | `import` + `components` / `app.component` |
| 文件组织 | 多个组件全堆一个 html/js 里         | 每个组件一个单独文件                      |
| 是否主流 | 多用于教学、简单 demo               | 正式项目标配                              |

**脚手架: vite,CLI等**

| 脚手架      | 主流程度               | 说明                       |
| ----------- | ---------------------- | -------------------------- |
| **Vite**    | ⭐⭐⭐⭐⭐（Vue3 默认推荐） | 极快、极轻量，Vue 官方维护 |
| **Vue CLI** | ⭐⭐（老项目用较多）     | Vue2 时代主力              |

```js
01_src_分析脚手架/
├─ node_modules/       //所有依赖包都装在这里
├─ public/             //放静态资源,index.html
├─ src/
│  ├─ components/       // 组件/页面代码
│  │   └─ App.vue
│  └─ main.js        //入口 JS 文件
├─ .gitignore
├─ babel.config.js
├─ package.json       //记录项目的依赖、脚本命令、项目名称等
├─ package-lock.json
├─ README.md
└─ vue.config.js
```

```js
ref属性:
1.ref="xxx"：给某个 DOM 或子组件起一个名字
2.this.$refs.xxx：通过这个名字，在 JS 中拿到对应的 DOM 或子组件对象 (vue2)
3.$ 是 Vue 内部给“实例属性”的统一前缀
4.Vue3 中的 DOM ref → xxx.value

<School ref="sch" />
this.$refs.sch     // 拿到子组件实例
this.$refs.sch.msg // 调子组件的数据
this.$refs.sch.someMethod() // 调子组件的方法
```

```js
props属性: 父传子,父组件通过 HTML 属性向子组件传值，子组件用 props 接收，类型可校验，可设默认值
1. vue2写法
父组件:<School :name="schoolName" />
子组件:props: ['name']
或   props: {
      name: {
        type: String,
        default: '未命名'
      },
      age: {
        type: Number,    //类型
        required: true   //默认值
      }
    }
2.vue3写法:
<script setup>
const props = defineProps({
  name: String,
  age: Number,
  data: Object
})
</script>

<template>
  <h1>{{ name }}</h1>
</template>
```

```js
1.scopoed样式:用来让当前组件的样式只作用于当前组件，不会影响别的组件
样式只在此组件生效
<style lang="less" scoped>
h1 {
  color: red;
}
</style>
2.lang="xxx" 表示 <style> 用的不是普通 CSS，而是某种“增强版 CSS 语言”
lang="less" → 使用 LESS 预处理器
lang="scss" → 使用 SCSS 预处理器
```

```js
组件自定义事件:子传父
1.绑定事件
子组件用 defineEmits 或 emits 选项声明事件名称
触发事件用 emit('事件名', 参数...)
父组件用 @事件名="方法" 来绑定事件处理函数

子组件:const emit = defineEmits(['eventName'])
      emit('eventName', payload)
父组件:<Child @eventName="handler" />
      handler(payload) { ... }

2.解绑事件
a. vue2里常用
$on / $off 事件总线写法
@xxx.native 修饰符
mitt
b. vue3使用
onMounted + onUnmounted
```

```js
$nextTick:把页面 DOM 真正改完，再叫我干活,就是下一次DOM更新结束后,再执行回调
1.vue2:this.$nextTick(() => {...})
2.vue3:nextTick(() => {...})   //用函数

基于更新后的 DOM 做操作：
自动聚焦 / 滚动到某位置
```

## 七、配置代理

-   **代理存在的核心目的：解决跨域 + 开发方便,端口不同会跨域CORS问题**

-   **反向代理: 客户端以为在访问一个服务器，实际上这个服务器把请求分发给真正的后端服务。**
-   **devServer / Nginx / 网关，大多都在做“反向代理”**

```js
/*
target：要转发到的后端地址
pathRewrite / rewrite：重写路径，常配“去掉前缀”
changeOrigin：是否改 Host，解决某些后端对域名的限制
ws：是否代理 WebSocket
*/
// vite.config.js
export default {
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:5000',
        changeOrigin: true,
        rewrite: path => path.replace(/^\/api/, '')
      }
    }
  }
}
```

## 八、插槽

-   **默认插槽（default slot）**
-   **具名插槽（named slot）**
-   **作用域插槽（scoped slot）**

```js
1.默认插槽
<slot>我是默认内容</slot>
2.具名插槽
子:  <slot name="header">默认标题</slot>
父:  <template #header>
    <h1>这是自定义头部</h1>
     </template>
3.作用域插槽
子: <slot :item="item">
   {{ item.name }}  <!-- 父组件没提供内容时的默认显示 -->
 </slot>
父: <List :items="users">
  <template #default="{ item }">
    <strong>{{ item.name }}</strong> - {{ item.age }}岁
  </template>
</List>
```

## 九、路由

-   **概念:**在单页应用 SPA 里，其实只有一个 `index.html`，我们希望像多页面网站一样：

    -   `/home` 显示首页组件
    -   `/about` 显示关于页组件

    这个“**路径 -> 组件**”的映射关系，就是“路由”。
     而管理这些关系的一整个“系统”，就是 **Vue Router**

-   路由表是一个**数组**，里面每一项是一个“路由记录（route record）”

    >   路径 `/xxx` 对应要渲染哪个组件

```js
const routes = [
  {
    path: '/home',      // URL 地址
    component: Home     // 要展示的组件
  },
  {
    path: '/about',
    component: About
  }
]
```

-   **核心标签: **`<router-view>` & `<router-link>`
    -   `<router-view>`：**显示当前路由对应的组件**,是一个“占位符”，当前 URL 对应的组件会被塞到这里来显示
    -   `<router-link>`：**在页面里跳转路由的链接**,做**前端无刷新跳转**，代替 `<a>` 标签
        -   `replace`属性,替换当前记录,默认为`push`

```js
<template>
  <div>
    <!-- 这里就是页面主要显示区域 -->
    <router-view />
  </div>
</template>

<template>
  <div>
    <router-link to="/home">去首页</router-link>
    <router-link to="/about">去关于</router-link>
  </div>
</template>
```

-   **动态路径参数：**`params`

    > 路径的一部分是“变量”，比如：`/user/1`,`/user/2`它们都要走同一个组件，只是 id 不一样。

```js
//配置
{
  path: '/user/:id',   //若使用to的对象写法,需要此处改为name配置
  component: User  
}
//访问
// 在 User 组件里
// Vue3 + Vue Router4 一般这样用：
import { useRoute } from 'vue-router'
const route = useRoute()
console.log(route.params.id)  // 比如 "1"
```

-   **查询参数：**`query`

>   像 `?page=2&keyword=vue` 这种在 URL 后面用 `?` 拼的，就是 query 参数

```js
//跳转方式,对象写法
<router-link :to="{ 
	path: '/list', 
	query: { 
        page: 2, 
        keyword: 'vue' 
    	    } 
}">
去第2页
</router-link>
//在组件里读取：
const route = useRoute()
console.log(route.query.page)     // "2"
console.log(route.query.keyword)  // "vue"
```

-   **路由模式: **`hash` 和`history`

    -   **hash 模式**（`/#/home`）

        >   http://example.com/#/home,`#` 后面的部分（`#/home`）后端服务器基本不管,刷新时不会 404，兼容性好,默认

    -   **history 模式**（`/home`）

        >   http://example.com/home,刷新时需要后端配合，把所有路径都指向同一个 `index.html`

-   **嵌套路由(子路由)**

```js
//配置
const routes = [
  {
    path: '/user',
    component: UserLayout,   // 外壳布局
    children: [
      {
        path: 'profile',     // 注意这里没写 /
        component: UserProfile
      },
      {
        path: 'posts',
        component: UserPosts
      }
    ]
  }
]	
//在UserLayout组件里
<template>
  <div>
    <h2>用户中心</h2>
    <!-- 子路由展示的位置 -->
    <router-view />
  </div>
</template>
```

-   **编程式导航：用代码跳转**(不用写 `<a>`，直接在逻辑代码里控制页面跳转)

>   不是点 `<router-link>`，而是在 JS 里写代码跳转，比如点击按钮后跳转详情页

```js
import { useRouter } from 'vue-router'

const router = useRouter()

function goDetail(id) {
  router.push(`/detail/${id}`)
  // 或者：
  router.push({ name: 'detail', params: { id } })
}
```

-   **当前路由对象:`route`: 当前 URL 的所有信息会被封装在一个 `route` 对象里，比如：**

```
route.path 当前路径 /user/10
route.params 动态参数 { id: '10' }
route.query 查询参数 { page: '2' }
```

```js
//获取
import { useRoute } from 'vue-router'
const route = useRoute()
console.log(route.path)
console.log(route.params)
console.log(route.query)
```

-   **路由守卫**

>   页面跳转前后，可以拦一下，检查下再决定放不放行

## 十、Vue3

-   **Composition API**（组合式 API）：用 `setup()`、`ref`、`reactive` 写逻辑

-   **Vue2：** `new Vue({...}).$mount('#app')`,用 `data、methods、computed、watch` 分块写
-   **Vue3：** `createApp(App).mount('#app')`,用 `setup()` 或 `<script setup>` 一块写,生命周期钩子在 setup 里用 onXXX 来写。

```bash
vite创建Vue3工程步骤:
npm create vite@latest #交互式选择框架
npm create vite@latest my-vue-app -- --template vue  #直接指定 Vue 模板
npm create vite@latest my-vue-app -- --template vue-ts   #Vue + TS
cd my-vue-app
npm install
npm run dev
npm run build   # 打包
npm run preview # 本地预览打包结果
```

```txt
工程结构:
my-vue-app/
├─ index.html        # 唯一 HTML 入口页面
├─ vite.config.js    # Vite 配置（代理、别名等）
├─ package.json      # 项目名、依赖、scripts 脚本
├─ node_modules/
├─ public/
└─ src/
   ├─ main.js
   ├─ App.vue
   ├─ assets/
   │   └─ vue.svg
   ├─ components/
   │   └─ HelloWorld.vue
   ├─ router/        # (如果你加了路由)
   ├─ stores/        # (如果你用 Pinia)
   └─ styles/        # (全局样式，比如 main.css)
```

```js
src/ 目录的核心
// src/main.js
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'          // 如果有路由
import { createPinia } from 'pinia'    // 如果用状态管理
import './styles/main.css'            // 全局样式

const app = createApp(App)
app.use(router)
app.use(createPinia())
app.mount('#app')

//App.vue —— 整体布局 / 根组件
<template>
  <RouterView />
</template>

<script setup>
// 一般不会写太多逻辑，这里更像是布局壳子
</script>

<style scoped>
/* 可以写一些全局布局相关样式 */
</style>

//components/ —— 通用小组件
<script setup>
import UserCard from '@/components/UserCard.vue'
</script>

<template>
  <UserCard :user="user" />
</template>
```

-   `setup()` **里最常用API**

    -   `ref()` —— 定义 **基本类型** 的响应式数据（数字 / 字符串 / 布尔 / null / undefined）

        >   使用规则：ref → .value 

    -   `reactive()` —— 定义 **对象、数组、字典、复杂数据结构**

        >   reactive 生成的是一个 **Proxy 代理对象**，直接修改属性就能响应式

    -   `setup()` 参数

```js
setup(props, context) {     //在 <script setup> 里参数自动注入
    
}
context参数包括: 
1.emit	发出事件
  slots	插槽
  attrs	透传的属性
```

-   `computed` 和 `watch`

    ```js
    import { computed, watch } from 'vue'
    
    const x = computed(()=>{})
    const fullName = computed({
      get() { ... },
      set(v) { ... }
    })
    
    // 监听 ref
    const count = ref(0)
    watch(count, (newV, oldV) => {
      console.log(newV, oldV)
    })
    // 监听 reactive  某个属性
    const state = reactive({ age: 18 })
    watch(
      () => state.age,
      (newV, oldV) => {
        console.log('age changed')
      }
    )
    //监听整个对象,需要 deep: true
    watch(
      state,
      (newV, oldV) => {
        console.log('any field changed')
      },
      { deep: true }
    )
    
    () => user.name
    [count, () => user.name]
    ```

-   **Vue3生命周期**

```js
1.setup：Vue3 的“起点”,拿到 props,可拿到 emit、slots、attrs
2.onMounted：常用钩子
onMounted(() => {
  console.log('初次高度：', boxRef.value.offsetHeight)
})
3.onUpdated：数据变了、DOM 更新完了再干点事
onUpdated(() => {
  console.log('更新后的高度：', boxRef.value.offsetHeight)
})
4.onUnmounted：组件离场善后
onUnmounted(() => {
  console.log('组件被卸载了，清理定时器')
  clearInterval(timer)
})
5.onBefore* 系列
onBeforeMount
onBeforeUpdate
onBeforeUnmount
```

-   `toRef`：把“一个属性”变成一个`ref`, 从一个 **响应式对象**（一般是 `reactive` 出来的）里，**取出某个属性**，把它变成一个 `ref`，而且两边是**联动的**
    -   第一个参数必须是 **响应式对象**（`reactive` 或 `props` 等）
    -   第二个参数是字符串，表示属性名

-   `toRefs`：把“整个对象的每个属性”都变成 `ref`, 把一个响应式对象（`reactive`）**按属性批量拆成一堆 `ref`**，避免解构丢失响应式

    ```js
    import { reactive, toRef ,toRefs} from 'vue'
    
    const person = reactive({
      name: '张三',
      age: 18
    })
    // 从 person 里单独拿一个属性出来
    const ageRef = toRef(person, 'age')
    // 改 ageRef，会影响 person.age
    ageRef.value++       // person.age 变成 19
    // 改 person.age，也会影响 ageRef.value
    person.age = 20      // ageRef.value 也变成 20
    return {
      ...toRefs(person)
    }
    ```

-   `provide` / `inject` : **跨层级通信**

    >   父组件提供（provide）一些数据，任意深度的子组件都可以直接注入（inject）来使用，不需要一层层 props 传递。

```js
provide('menuContext', {
  activeKey,
  setActiveKey,
})

const { activeKey, setActiveKey } = inject('menuContext')
const handleClick = () => setActiveKey(props.key)
```

# Maven

-   Maven的核心概念是**项目对象模型（Project Object Model，POM）**，它是一个XML文件，也是 Maven 项目的核心文件，定义了项目的配置、依赖、插件以及构建的过程。
-   Maven中有一个仓库的概念，仓库简单来说就是指存放jar包的地方，按照作用范围的不同可以分为**本地仓库、远程仓库(私服仓库)和中央仓库。**

```bash
# 标准的Maven工程目录结构：
project                     # 项目根目录
|-- src                     # 源代码目录
|   |-- main                # 主目录
|   |   |-- java            # Java源代码目录
|   |   |-- resources       # 资源文件目录
|   |   |-- webapp          # Web应用目录
|   |-- test                # 测试目录
|       |-- java            # 测试源代码目录
|       |-- resources       # 测试资源文件目录
|-- target                  # 项目构建目录
|-- pom.xml                 # 项目配置文件
```

| 阶段           | 处理         | 描述                                                     |
| -------------- | ------------ | -------------------------------------------------------- |
| `mvn validate` | 验证项目     | 验证项目是否正确且所有必须信息是可用的                   |
| `mvn compile`  | **执行编译** | 源代码编译在此阶段完成                                   |
| `mvn test`     | 测试         | 使用适当的单元测试框架（例如JUnit）运行测试。            |
| `mvn package`  | **打包**     | 将编译后的代码打包成可分发的格式，例如 JAR 或 WAR        |
| `mvn verify`   | 检查         | 对集成测试的结果进行检查，以保证质量达标                 |
| `mvn install`  | **安装**     | 安装打包的项目到本地仓库，以供其他项目使用               |
| `mvn deploy`   | **部署**     | 拷贝最终的工程包到远程仓库中，以共享给其他开发人员和工程 |

-   **Nexus**是最常用的Maven私服仓库，可以用来存放公司内部的jar包，以及一些第三方的jar包，这样就可以避免重复下载，提高构建速度。

# Springboot+SSM

```java
project-root
│
├── src
│   ├── main
│   │   ├── java
│   │   │   └── com
│   │   │       └── staybun
│   │   │           ├── controller
│   │   │           ├── service
│   │   │           ├── dao
│   │   │           └── entity/pojo
│   │   └── resources
│   │       ├── application.yml
│   │       └── mappers
│   │
│   └── test
│       └── java
│
└── pom.xml
    
package com.staybun.service;	//  src/main/java/com/staybun/service

```

-   **注解**: 就是贴个备注,框架根据注解做额外的事情

```java
@RestController   //Web,http请求
@GetMapping("/hello")  //路由映射
```

-   **REST API 规范**

>   用 URL 表示资源（名词），用 HTTP 方法表示动作（动词）
>
>   GET 查，POST 增，PUT 替换，PATCH 修改，DELETE 删

```java
1.注解
它贴在哪？（类 / 方法 / 字段）
是谁在读它？（Spring / JPA / Lombok）
它解决了什么问题？
Bean
@Component	// Ioc
@Autowired  // 依赖注入,自动装载
@Qualifier	// 明确指定要注入哪个 Bean
@Override	// 接口功能的实现
@Aspect		// 声明此类是切面对象
	JoinPoint:joinPoint.getSignature().getName();	//方法名
			  joinPoint.getArgs();	//方法参数
@Before("execution(* com.staybun.service.*.*(..))")	//在满足这个 execution 规则的方法执行【之前】，执行我这个方法
@AfterReturning
@After
@Around
@Service	//声明业务层组件
@Resource	//注入依赖对象
@RequestMapping		//配置接口路径（路由映射）
```

-   **IoC（控制反转）**
-   **依赖注入（DI）**
-   **解耦（Loose Coupling）**
-   **AOP（面向切面编程）**

-   **事务**: 一组操作，要么全部成功，要么全部失败

    | CRUD | 英文   | SQL      | MyBatis / Java 常见方法名    |
    | ---- | ------ | -------- | ---------------------------- |
    | 增   | Create | `INSERT` | `insertUser()` / `save()`    |
    | 查   | Read   | `SELECT` | `selectById()` / `findAll()` |
    | 改   | Update | `UPDATE` | `updateById()`               |
    | 删   | Delete | `DELETE` | `deleteById()`               |

| 类型         | 本质        | 一句话理解                            |
| ------------ | ----------- | ------------------------------------- |
| `properties` | 键值对      | 简单直给，老实人                      |
| `yml / yaml` | 层级结构    | 清晰好读，现代化,Spring Boot 官方推荐 |
| `xml`        | 结构 + 规则 | 适合“复杂规则型配置”                  |

-   **Mybatis**: 把 SQL 写在（XML/注解），把结果映射成 Java 对象，管理 JDBC 的一堆脏活累活
    -   **SqlSessionFactory / SqlSession**
    -   **Mapper（映射器接口）与映射文件（XML/注解）**
    -   **参数绑定：#{ } vs ${ }**
    -   **结果映射 ResultType / ResultMap**
    -   **动态 SQL（if/where/foreach）**
    -   一级缓存 / 二级缓存



